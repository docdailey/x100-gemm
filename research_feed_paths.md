# research_feed_paths.md — How to feed SpaceMIT K3 / Milk-V Jupiter 2 better

**Role:** research note for the decode throughput effort. Complements `codex_recs_1.md` (implementation roadmap) and `docs/HARDWARE.md` (measured map).  
**Question:** is there a path past “CPU `vle8` + plain `vmadot` + DRAM stream” that actually uses this hardware better?  
**Status:** hypotheses + external evidence + in-repo vendor evidence. **Fill measured numbers into the tables as probes land.** Do not treat this file as proof until board results are written in.

---

## 1. Executive synthesis

Three layers matter. There is **no** credible evidence of a secret 30–60 TOPS decode path past LPDDR. There **is** strong evidence the custom pure-C path uses only a **subset** of IME-2, while SpacemiT’s own kernels use native int4/i2 ISA forms, N32 panel kernels, in-loop scale folding, and a persistent A100 worker model.

| Layer | Missing / underused relative to vendor | Likely impact on decode tok/s |
|-------|----------------------------------------|-------------------------------|
| **Kernel/ISA** | `vnpack4`, typed `vmadot … i4`, `vmadotsu.hp`, i2×i8, N32K32 panels, in-register fp16-scale fold | Potentially large removal of current fold/dispatch overhead; **not** a reduction below W4 weight bytes |
| **Runtime** | Persistent A100 workers, spin barriers, tile queue, pack-once | Large for MoE (many small GEMVs) |
| **Product** | Spec / ngram / M-batch so one W stream yields more accepted tokens | Largest *useful* tok/s (external proof) |

Marketing “>10 tok/s @ 30B” is **MoE + good software and/or accepted-token amplification**, not a magic matmul. External Jupiter 2 measurements report roughly 6.5–7.2 raw decode tok/s for a ~3B-active quality MoE and 15.5 tok/s only on a copy-heavy ngram-speculative workload with 81% acceptance.

### Answer to “can this take us further after the current path?”

**Yes, but not every path is equally credible.** Once `codex_recs_1.md` PR1–PR3 have produced a trustworthy profile, the best next kernel experiment is a minimal port of the active vendor `gemm_kernel_i8i4_m1` shape. It directly combines three things the current engine lacks:

1. one activation group reused across an N32 output panel;
2. native `.hp` int4 operations; and
3. per-group fp16 scale application into fp32 accumulators before the next K group.

That can remove much of the scalar group-fold path and reduce activation loads and output-store traffic. It cannot evade reading the W4 weights from memory, so the end state is still bounded by packed-weight bandwidth. After kernel/runtime parity, M-batching and ngram/speculative verification are the credible ways to go beyond single-token bandwidth limits.

Do not make TCM/ai_dma the automatic next project. It advances only if cold-stream overlap measurements beat direct DRAM streaming end to end.

---

## 2. External ground truth (same SoC class)

Source: [Tao of Mac — MilkV Jupiter 2 / SpacemiT K3](https://taoofmac.com/space/reviews/2026/06/11/1830) (Jun 2026), SpacemiT / fork llama.cpp stacks.

| Workload | Stack | Decode (reported) |
|----------|--------|-------------------|
| TinyLlama 1.1B Q2_K | `llama.cpp-tools-spacemit` | ~36.6 tok/s |
| Qwen3 0.6B | SpacemiT llama.cpp | ~43–55 tok/s |
| Qwen3.6-28B-REAP-A3B (~3B active) | same | ~6.5–7.2 tok/s |
| Same MoE + ngram draft (~81% accept) | same | ~15.5 tok/s |
| Gemma 4 E2B QAT | same | ~12.9 tok/s |

**BW-consistent story:** ~3B active × ~0.5–0.625 B/weight ≈ 1.5–1.9 GB/token, before attention, lm-head, padding, and metadata. The external 6.5–7.2 tok/s result is a useful software target, not proof that the same rate follows from a simple DRAM-GB/s division. If pure-C MoE remains far below that on the **same GGUF, context, thread map, and sampling settings**, the gap is primarily software path rather than undiscovered silicon peak.

**Also reported (and important negatives):**

- X100 draft + A100 target MTP: **no** decode win (no free fast fabric between clusters).
- Decode tracks **1 / active parameters** (memory-bound), not peak IME TOPS.
- Linlon V5 / Zhouyi NPU appears in DT; **no public driver/SDK** → dead for now.
- PowerVR OpenCL exposes `integer_dot_product`; interesting for vision/research, not the measured LLM path.

---

## 3. What this repo already proved

See `docs/HARDWARE.md`, `bench/EP_INVESTIGATION.md`, `bench/*`.

| Fact | Implication for “feed” |
|------|------------------------|
| A100 unlock: `/proc/set_ai_thread` then affinity | Required for any IME-2 userspace path |
| IME-2: 4 units, pairs (8,9)…(14,15); VLEN 1024; tile 8×8×16 int8 | Resource is units + BW, not core count alone |
| Register-fed ~14.6 TOPS int8 | **Not** decode ceiling |
| L1-fed GEMM ~2.3 TOPS; vendor EP ~2.8 TOPS @ ~1 core | Real matmul ceiling class; no 60 TOPS path |
| EP uses `/dev/tcm`, `/dev/ai_dma`, mmap ring/doorbell | Offload exists; RE is large; still ~2.8 TOPS class |
| Decode is weight-stream bound | Bytes/token and achieved GB/s dominate |
| q4-in-q8 interleave is **correct** and beats int8 BW | Good intermediate design; may not be native-optimal |

---

## 4. Path A — Vendor-shaped IME-2 M1×N32 int4 kernel (strongest next kernel hypothesis)

### 4.1 What the custom engines do today

- Requant to signed int4, **software** pack two K16 tiles into 128 B.
- Unpack with `vsll` / `vsra`.
- Plain `vmadot` (int8-shaped operands).
- M=1 decode pads 7 of 8 tile rows with zeros.

### 4.2 What vendor kernels in-tree already do

Evidence: `reference/spacemit-backend/ime2_kernels.cpp`.

**Native nibble / typed-dot operations:**

```asm
vnpack4.vv   v8, v3, v3, 3          # lo4 of A
vnpack4.vv   v10, v24, v24, 3       # hi4 of A
vmadotsu     v16, v10, v4, i4       # typed int4 MAC
vmadotu      v16, v8, v4, i4
vpack.vv     ...                    # rearrange half-results
```

**Half-path multi-acc form and low-bit modes:**

```asm
vmadotsu.hp  v24, v2, v8, v0, 0, i8
# ... and i2 × i8 kernels with vmadotsu.hp ...
```

**B layout and scale fold (comments and active assembly in the same file):**

```text
A:  M1 × K32 int8     (256-bit) + fp32 scale + int16 asum
B:  N8 × K32 int4     (1024-bit full VLEN; e.g. vl4r.v load)
    + fp16 scales for N32 panels
```

The important local evidence is stronger than the public narrative: in `gemm_kernel_i8i4_m1`, the ordinary typed-`i4` implementation is under `#if 0`, while the `.hp` implementation in the `#else` branch is active. It processes N in panels of 32, loads four vector registers of B data, loads 32 fp16 B scales, executes the typed int4 dot sequence, converts/scales inside the K-group loop, and retains an fp32 N32 accumulator until the final store.

The `vnpack4` operations split the int8 activation into nibble contributions used to reconstruct its signed value against packed int4 B. They do **not** further compress the already-int4 weight stream.

### 4.3 Why this might beat q4-in-q8 interleave

| | Custom interleave | Vendor-shaped M1×N32 int4 |
|--|-------------------|--------------------|
| Output panel | N8 | N32 (four N8 blocks) |
| Activation use | two padded 8×16 A loads per N8/K32 | one M1K32 activation group reused across N32 |
| Weight bytes | 128 B per N8K32 | 512 B per N32K32 — **same 0.5 B/weight** |
| Dot/fold | two int8-shaped `vmadot`, store 8×8 int32 each group, scalar fp32 fold | typed `.hp` int4 sequence, fp16 B-scale multiply, fp32 accumulation inside K loop |
| Scale stream | separate fp32 `gs[]` = 0.125 B/weight | fp16 group scales = 0.0625 B/weight |
| Final output | group partial stores plus scalar reduction | one final N32 fp32 store |

Hypothesis: interleave is a **correct compatibility shim**; the vendor shape wins primarily through N32 activation reuse and an in-register group-scale fold, with a secondary scale-bandwidth win from fp16. It does **not** halve W4 data again. This distinction matters: if the current full-token profile is already dominated by cold packed-weight reads, the native kernel’s ceiling gain will be limited; if scalar fold/dispatch dominates, it can be substantial.

### 4.4 Probe plan (falsifiable)

| ID | Probe | Pass / fail | Result (fill in) |
|----|--------|-------------|------------------|
| A1 | Assemble+run `vnpack4.vv`, typed `vmadotsu … i4`, and `vmadotsu.hp` on A100 | No illegal insn; record compiler/binutils and encodings | |
| A2 | Extract/port the minimal active M1×N32K32 `.hp` kernel and its exact A/B pack | Correct against fp32/grouped oracle | |
| A3 | Hot A/B: vendor shape vs custom interleave at identical N/K/data | Split kernel, fold, and total time | |
| A4 | Cold/model-stream A/B with identical W4 values and scales | Report µs, **physical bytes/s**, useful weights/s, correctness | |
| A5 | Full-token substitution on the same cached model | tok/s and bucket changes | |

**Do not** rewrite production engines until A1–A4 land. Do not compare the two kernels using only “effective int8 GB/s”; report actual bytes read for packed weights and scales.

### 4.5 Risk / benefit

| Benefit | Risk |
|---------|------|
| Removes scalar group fold and reuses A across N32 | Encoding may not match Bianbu as; exact register/pack contract is delicate |
| Aligns pack format with SpacemiT llama.cpp / ONNX | Different cache format; version requant caches |
| fp16 scales reduce scale bytes | Rounding/quality differs from current fp32 scales |
| i2 path for selected tensors | Severe quality cliffs; separate research after int4 parity |

---

## 5. Path B — Runtime feed (persistent A100 team)

### 5.1 Vendor / SpacemiT llama.cpp pattern

- Persistent pthreads on A100 (the external report describes **six workers on harts 8–13**; verify the referenced fork/configuration locally rather than treating six as universally optimal).
- `spine_barrier_t` (atomic spin barrier) — see `reference/spacemit-backend/spine_barrier.h`.
- Shared threadpool/tile work; workers remain alive between operations.
- TCM paths in `reference/spacemit-backend/ime.cpp` use `rvv::memcpy1d` and `spine_barrier_wait` to stage panels. This is local evidence for CPU-copy/barrier staging, not proof that `ai_dma` is used by this llama.cpp path.

Tao of Mac reports 36.6–55 tok/s on small Qwen/TinyLlama cases and attributes part of the result to the persistent worker model. Treat attribution as an external observation; the same source reports only about 6.5–7.2 raw decode tok/s on the ~3B-active quality MoE.

### 5.2 Custom engines today

- `#pragma omp parallel` **per** `lin_mm`.
- Re-quant of activations **inside** each linear.
- Hot `malloc` of group partials (to be removed per `codex_recs_1.md`).

### 5.3 Hypothesis

For MoE (router + 8×3 expert GEMVs + attn), **dispatch overhead and re-pack** dominate a non-trivial fraction of token time. Microkernel ILP cannot fix that.

### 5.4 Probe plan

| ID | Probe | Result (fill in) |
|----|--------|------------------|
| B1 | Empty OpenMP region + barrier cost × the measured number of `lin_mm` calls | µs / token |
| B2 | Many-small GEMV (MoE-shaped) OpenMP-per-call vs persistent pool | |
| B3 | Full-token profile: pack count, parallel-region count | |
| B4 | 4-across (8,10,12,14) vs 6-worker vs 8-paired on **cold** W | tok/s, GB/s |

Aligns with codex PR2/PR5/PR8 — measure first, then persistent pool only if B1–B2 show material overhead.

---

## 6. Path C — TCM + ai_dma (real, gated)

### 6.1 What is real

- ~3 MB on-chip TCM; pair-associated blocks (order ~8 × 384 KB).
- `libspine_tcm.so` / `spine_tcm.h` API in-tree.
- EP opens tcm + **ai_dma** + aidma_list + dma_msi; submission via **mmap ring** (no per-xfer ioctl storm).
- llama.cpp spacemit path often uses **CPU `memcpy1d`** into TCM rather than full DMA RE.

### 6.2 What is oversold

| Claim | Caution |
|-------|---------|
| ~58 GB/s spine_tcm read | Likely **warm cache / L2**; codex_recs requires cold-stream proof |
| Docs ~5.4 GB/s A100 TCM | Scratchpad class, not second DRAM |
| Fit 8 MoE experts in TCM | One expert gate+up+down W4 ≈ **2.4–3 MB** (with scales) → nearly all of TCM; **panels / double-buffer**, not whole expert set |

### 6.3 Interesting pattern (not “infinite SRAM”)

```text
hart pair unit:
  buf A: compute from TCM
  buf B: DMA or memcpy next weight panel from DRAM
  spine barrier → swap
```

Attempts to hide DRAM latency behind IME work. It helps only if copy and compute genuinely overlap and measured **full-token** time drops. CPU `memcpy1d` consumes load/store and memory bandwidth; it is not automatically free just because another hart performs it.

### 6.4 Probe plan

| ID | Probe | Result (fill in) |
|----|--------|------------------|
| C1 | First-touch TCM read BW; working set > L2; cold vs warm | GB/s |
| C2 | DRAM→TCM memcpy BW + latency | |
| C3 | Overlap: partner memcpy while compute on other hart; compare direct-DRAM kernel | full GEMV µs, total DRAM bytes, worker CPU time |
| C4 | ai_dma ring: objdump EP + one scripted submit (research branch) | |

**Gate:** do not build production decode on TCM until C1–C3 are written. Prefill offload via EP remains the lower-risk path for large matmuls (~2.8 TOPS class).

---

## 7. Path D — Product-level feed (same silicon, more useful tokens)

These do not raise peak TOPS; they raise **accepted tokens per second** or **tokens per weight stream**.

| Idea | Mechanism | External signal |
|------|-----------|-----------------|
| **M-batch independent sequences** | One W stream, M≤8 rows of IME tile | Tile geometry free rows today |
| **Expert-grouped MoE** | Stream expert e once for all tokens that selected e | Serving / multi-seq |
| **Ngram / draft spec** | Verify many drafts per W pass | ~15.5 tok/s on 28B-REAP class |
| **Native GGUF→IME pack** | Avoid dequant→requant→custom pack | Vendor cache format |
| **fp16 group scales** | 0.5625 vs 0.625 B/weight | Vendor B scales are fp16 |
| **i2 on selected tensors** | Extreme density | Vendor i2×i8 kernels exist |

The cited external experiment found no benefit from placing an assistant drafter on X100 while the target used A100. That result does not prove there is literally no fast fabric; it proves that tested scheduling did not improve end-to-end decode. Keep draft/verify within one coordinated runtime unless a new end-to-end measurement says otherwise.

---

## 8. Dead or weak paths (do not prioritize)

| Path | Verdict |
|------|---------|
| Secret 60 TOPS real matmul | Killed by EP ~2.8 TOPS + L1 ceiling ~2.3 |
| Linlon V5 NPU userspace | No driver/SDK |
| More register-fed `vmadot` peak | Orthogonal to decode BW |
| Cross-cluster draft/verify for free speedup | Measured no win |
| Raw uncached `/dev/tcm` CPU `vle8` | ~0.4 GB/s class — anti-pattern |

---

## 9. Ranked research agenda

| Rank | Focus | Decides | Owner suggestion |
|------|--------|---------|------------------|
| **R0** | Finish codex timing buckets + same-GGUF SpacemiT baseline | Establish the gap and its location | Required gate |
| **R1** | Assemble/run probe for `vnpack4` / typed `i4` / `.hp` | Is Path A available in this toolchain? | Small board binary |
| **R2** | Minimal active vendor M1×N32 kernel + correctness oracle | Can it reproduce grouped W4 output? | Standalone benchmark |
| **R3** | Cold vendor-shape vs `q4_gemv` A/B | Keep interleave or migrate pack? | Physical-byte and time table |
| **R4** | Full-token kernel substitution | Does the micro win survive engine glue? | Engine feature flag |
| **R5** | Persistent workers vs OpenMP (B-probes) | Whether PR8 is warranted | |
| **R6** | M-batch / expert grouping / ngram spec | More tokens per W stream | Product path |
| **R7** | Cold TCM + pair double-buffer (C-probes) | Whether staging beats direct DRAM | Research only |

---

## 10. Relationship to `codex_recs_1.md`

| codex theme | This research note |
|-------------|-------------------|
| Measure first; don’t assume mid-loop store vanishes | Still true; Path A is **additional** ISA surface |
| W4 ≈ 0.625 B/weight with fp32 scales | Path D: fp16 scales as later density knob |
| TCM 58 GB/s not streaming | Path C gate |
| M>1 not validated in `q4_gemv` | Path D depends on fixed M-oracle |
| Persistent pool only after overhead measured | Path B |
| Prefill via EP ~2.8 TOPS | Unchanged |

**Do not** reorder codex PR1–PR3 for Path A/C. Small illegal-instruction probes may run at any time, but the result that matters is a cold, same-data A/B after the profiler exists. Merge ISA changes only after A1–A5. Path C remains behind the stronger cold-stream gate.

---

## 11. In-repo and external source index

| Source | Use |
|--------|-----|
| `reference/spacemit-backend/ime2_kernels.cpp` | Native int4/i2/`.hp`/`vnpack4` asm |
| `reference/spacemit-backend/spine_tcm.h`, `spine_barrier.h` | TCM API, worker sync |
| `reference/spacemit-backend/repack.cpp` | GGUF → interleaved/native pack |
| `docs/HARDWARE.md` | Measured topology / ceilings |
| `bench/EP_INVESTIGATION.md` | EP ~2.8 TOPS, ai_dma devices |
| `bench/q4_gemv.c` | Custom interleave oracle |
| [spacemit-com/riscv-ime-extension-spec](https://github.com/spacemit-com/riscv-ime-extension-spec) | Public IME proposal (int4/fp types) |
| [Tao of Mac K3 review](https://taoofmac.com/space/reviews/2026/06/11/1830) | End-to-end tok/s, worker/TCM narrative |
| SpacemiT / community llama.cpp K3 forks | Production baseline |

---

## 12. Results log (append only)

Copy a row per probe run. Prefer the fuller template in `codex_recs_1.md` §18 for engine tokens.

| Date | Probe ID | git SHA | Setup | Metric | Notes |
|------|----------|---------|-------|--------|-------|
| 2026-07-26 | A1 | 31504f6 | A100 hart 8, gcc 15.2.0 (Bianbu), binutils 2.46, `-march=rv64gcv_zvfh_xsmtvdotii` | illegal? N | `vnpack4.vv`, `vmadotsu`/`vmadotu ...i4`, `vmadotsu.hp`/`vmadotu.hp` all assemble+run with the *same* march token already used for plain `vmadot` — no new extension flag needed. `bench/vendor_ime_probe.c`. Side finding: vendor B int4 is **unsigned nibble (0-15) with a baked-in zp=8** (`vmv.v.i v28,8` + asum·zp correction), NOT signed two's-complement like our current `pack_w_int4` — A2's pack routine must differ from ours, can't reuse it. |
| 2026-07-26 | A2 | 31504f6 | Hand-ported `gemm_kernel_i8i4_m1` (.hp branch) from `reference/spacemit-backend/ime2_kernels.cpp`, own A/B pack per kernel comments | correct? PARTIAL/UNVERIFIED | Kernel executes deterministically (`bench/vendor_ime_a2_probe.c`) and single-element probes disproved the naive `raw-zp*asum` hypothesis (real: `raw+zp*asum`), but then found the **actual production A-pack** (`quantize_a_row_i8_hp` in `rvv_kernels.cpp`) uses a two-level fp16 scale (per-32 relative ratio x per-256 shared average) + a **pre-scaled `-asum*8` fp16 term**, not the fp32-scale/raw-int16-asum layout the kernel's own inline comments imply. The 5768-line reference file has multiple gemm_kernel/quantize_a_row variant pairs (RVV-intrinsic and raw-asm, HP and non-HP, per-quant-type); I paired the wrong two. **Do not trust this hand port's correctness** — needs either full call-graph tracing in `ime.cpp` to find the true pair, or `objdump` of the actually-compiled `libggml-cpu.so` as ground truth instead of the (possibly stale) reference source snapshot. |
| 2026-07-26 | A5-equiv | 31504f6 | Real vendor binary `/root/llama.cpp/build/bin/llama-bench` (SpacemiT fork, build 308f61c, IME2 on), same GGUF (`Qwen3-30B-A3B-Q4_0.gguf`), decode-only (`-p 0 -n 16`) | tok/s | **t=4: 11.71±0.19 tok/s. t=8: 12.89±0.03 tok/s** (2 reps each, reproducible) vs our pure-C engine's **1.49 tok/s** at t=4 — **~8x gap**. This sidesteps A2's fragile hand-port entirely: the real compiled vendor kernel was already on the board. `t=16` and the combined `pp+tg` benchmark mode both crash (`malloc(): invalid size`) — `/dev/tcm_sync_mem` doesn't exist on this board (`open() failed, errno=2`, silently falls back to heap; that fallback path appears to have a bug under more threads/back-to-back benchmarks). t=4/t=8 decode-only is stable. |
| 2026-07-26 | A2-corrected | ed8b89d | Traced `ime.cpp`'s dispatch (line ~276): for Q4_0/Q4_1, INTER_SIZE==256, count_m<4, the real call chain is `gemm_kernel_i8i4_hp` → **`gemm_kernel_i8i4_hp_m1`** (ime2_kernels.cpp:2883) — NOT `gemm_kernel_i8i4_m1`, which was A2's original wrong pairing. Paired with the real `quantize_a_row_i8_hp` (rvv_kernels.cpp:1989, ported to scalar C) for A and `make_block_q4_0x32` (repack.cpp:292, also ported) for B. | correct? **VERIFIED PASS** | `bench/vendor_ime_a2_full.c`: verbatim asm transcription of `gemm_kernel_i8i4_hp_m1`, faithful ports of both real pack routines, validated against an independent scalar dequant reference (built from the same quantized nibbles/scales, isolating kernel correctness from quant noise). N=32,K=256 random test: **max rel diff 2.3%**, consistent with expected fp16 intermediate-accumulation noise (the kernel accumulates in fp16 before final fp32 widen), not a bug. Byte offsets independently confirmed: the asm's `+272`/`+288` constants exactly match `q8_hp_blk_size(256,true,true)=290B`'s derived layout. Real B nibble-pairing (from `make_block_q4_0x32`) is adjacent-pair `{2j,2j+1}`, **not** the native-ggml `{j,j+16}` pairing A1/A2's first attempt assumed — that was the root cause of the original mismatch. |
| 2026-07-26 | A3 | ed8b89d | Hot, in-cache, kernel-only (no pack overhead), identical N=32 K=256, 200k reps, both on A100 hart 8 | ns/call | **ours (4x `gemv_nb_int4_grouped` N8K256): 2972.8 ns/call. vendor (`gemm_kernel_i8i4_hp_m1`, N32K256 in one call): 446.4 ns/call. 6.66x faster.** Close to the ~8x full-token gap from A5-equiv, meaning kernel microarchitecture (not ggml dispatch/threading overhead) is the dominant factor — confirms porting this kernel into our engine is the right lever. |
| 2026-07-26 | A5 (real) | 3131fef | `qwen_moe_hp.c` full engine integration (see A2-corrected/A3 rows), run against the real 30B-A3B model, nt=4 | tok/s + correctness | **6.19 tok/s, ' Tokyo' PASS, coherent generation matches the original engine exactly.** Up from our 1.49 tok/s (**4.16x**), still below the real vendor binary's 11.71-12.89 (A5-equiv). Requant into the new format took **1104.5s (~18.4 min)** — much slower than the old format's ~2 min parallel requant (real cost: more fp16 conversions + 8-subblock nesting per weight group); cache save 92.8s. Buckets at the time looked like `linear(kernel)` 38.4ms / `rest` 100.3ms=62% — **this bucket split was WRONG, see PR8 row below.** |
| 2026-07-26 | PR8 + bucket fix | 709f855 | Implemented the persistent spin-dispatch pool (threads created once, generation-counter dispatch instead of `#pragma omp parallel` per `Lin` call) per the A5 row's hypothesis. **Result: 6.19→6.53 tok/s, only ~6% gain — nowhere near the ~16 tok/s projected.** Investigating why led to a real bug, not a wrong hypothesis about the hardware: the new engine's `lin_mm()` wrapper (used for `o`, all 8 `ed[e]`, and `lm` — over a third of per-layer Lin calls) had **zero timing instrumentation**, unlike the old engine's version. All of that real linear-algebra time was silently falling into the `rest` bucket the whole time, making it look like 62%-of-wall "mystery overhead" when it was actually mostly real, correctly-attributed work. Fixed the instrumentation (ported the old engine's `_ta/_tb` pattern into the new `lin_mm`) and re-measured: **act-pack 16.6ms, linear(kernel) 58.7ms, attention 16.9ms, rest 59.3ms, wall 152.4ms → 6.56 tok/s.** `linear` and `rest` are now roughly tied as the two biggest buckets; `rest` is genuine scalar C (router matvec O(n_exp·d)=262144 mults/layer unvectorized, per-head RoPE+qk-norm ~4600 sin/cos calls/layer, SwiGLU ~6144 `expf` calls/layer), not dispatch overhead. **Retracting the "~16 tok/s, beats vendor" projection from the A5 row above** — it was built on a bucket count that was silently missing a third of the real work. |
| 2026-07-26 | Fine buckets + rope-cache + router RVV | fa3d897 | Split `rest` into `rope+qknorm`/`router`/`swiglu`/`rest(other)` to get real numbers before guessing further (rest(other) collapsed to 2.1ms — confirms the split is now essentially complete). Two fixes, both zero-approximation-risk (caching + exact vectorization, not lossy math): (1) RoPE cos/sin table was being recomputed via `powf`+`sinf`+`cosf` **per head per layer** (up to 1728x/token) despite depending only on `(hd,pos,base)` — identical across all heads/layers for a given token. Built once per `forward()` call instead. (2) Router matvec (128 experts x d=2048, ~1MB/layer, previously scalar) vectorized with RVV `vfmacc`+`vfredusum` (`vdot_f32`). | tok/s + correctness | **rope+qknorm 13.2→2.0ms (6.6x). router 29.4→18.7ms (1.6x — smaller than hoped, consistent with the router matrix being memory-bandwidth-bound, not compute-bound, so vectorizing the multiply doesn't fix the DRAM-streaming cost). Wall 154.4→132.6ms. 6.56→7.54 tok/s.** Correctness held: `' Tokyo'` PASS, generation identical (Brasília, Ottawa) — confirms `vfredusum`'s unordered reduction didn't flip any expert-selection tie. Cumulative from the session's original 1.49 tok/s baseline: **5.06x**. Fresh bucket ranking: linear(kernel) 58.5ms (44%) > router≈attention 18.7ms each > act-pack 17.3ms > swiglu 14.2ms (untouched) > rope 2.0ms > rest(other) 2.2ms. |
| 2026-07-26 | 8-hart pin fix + test | 6d93c72 | `pin_once`'s old formula (`8+(tn*2)%8`) only worked by luck up to nt=4 (gives harts 8,10,12,14 — one per IME-2 unit, per `docs/HARDWARE.md`'s 4-units/2-cores-each topology) and **collides** for nt>4 (tn=4 maps back to hart 8, same as tn=0). Replaced with a lookup table (`{8,10,12,14,9,11,13,15}`, indexed `tn%8`) that fills one-per-unit first, then paired partners — collision-free for any nt in 1..8, and nt=4 behavior is unchanged (regression-tested: 7.51 vs 7.54 tok/s, within noise). | tok/s + correctness | **nt=8 tested for real: 7.27 tok/s — slightly WORSE than nt=4's 7.51.** `linear(kernel)` went 58.7→62.9ms, i.e. *up*. Consistent with the router finding (memory-bandwidth-bound, not compute-bound) and `docs/HARDWARE.md`'s own peak-TOPS table (4-across-pairs→8-cores-all-pairs is only +11% even for pure register-fed compute) — for our actual small-per-call GEMV workload, doubling threads adds LPDDR5-bus contention without shrinking the real bottleneck. Correctness held either way (`' Tokyo'` PASS, identical generation) — the pinning fix itself is verified safe, it's just that more threads isn't the lever here. **nt=4 remains the right default.** |
| 2026-07-26 | Router-as-Lin fp16 experiment (hand-written vdot, NOT the pooled HP kernel) | 3afa0b1 | Router (128xd, ~1MB/layer, full fp32) is itself shaped like every other `Lin` in this engine — tried fp16-packed weight first (not int4), matching the repo's W-lower/A-higher convention (weight loses precision, activation stays fp32). **Caveat that matters:** this used a standalone hand-written `vdot_f16w_f32a`, run single-threaded on the main thread only — NOT the actual `lin_mm_hp`/pool-dispatch path every other `Lin` in this engine uses. Hit two real bugs before getting a clean result (both now documented as toolchain gotchas, see below): (1) a SIGSEGV, crashing before any reachable code executed, traced via `dmesg`+`strace` to gcc's RVV auto-vectorizer mangling a completely unrelated, innocent-looking scalar `f32->f16` conversion loop — fixed by isolating it into its own `__attribute__((optimize("no-tree-vectorize")))` function, same fix class as this repo's existing vmadot gotcha but triggered by plain 'v'-extension autovec, no custom instructions involved. (2) After that, wrong-but-not-crashing output (`' 乾坤'` instead of `' Tokyo'`) — the widening `vfwcvt.f.f.v v6,v30` instruction's SOURCE-type interpretation depends on the **active vtype at the instruction's execution**, not at the time v30 was loaded; had re-set vtype to e32m1 (for the `a` operand load) *before* executing the widening convert. Fixed by reordering to do the convert immediately after the e16mf2 load, matching the exact pattern already proven correct inside `gemm_kernel_i8i4_hp_m1`. | correctness + tok/s | **After both fixes: `' Tokyo'` PASS, identical generation. Validation: 0/1344 expert-set mismatches (12 prefill + 16 decode positions x 48 layers) vs the fp32 reference** — routing is not sensitive to fp16 weight precision for this model, at least at the top-8-selection granularity (logit deltas were reported at `%.5f`, likely hiding real magnitude — re-run with scientific notation before trusting "0.00000" as meaningfully zero). **Router bucket didn't move — 18.6ms fp16 vs 18.7ms fp32.** At the time this was read as disconfirming bandwidth-saturation and pointing at "loop-carried latency" as the bottleneck, and used to conclude W4 router wasn't worth pursuing. **That conclusion was premature and is retracted** — this experiment never tested the actual `lin_mm_hp` pooled/multi-threaded path (nt=4 parallel across N32 panels, the proven ~446ns/call kernel from A3); it only tested a single-threaded hand-written dot product, which is not representative of what a real HP-format router `Lin` would run through. "Loop-carried latency" was stated as a conclusion but is still just a hypothesis — never isolated/tested directly (e.g. via multiple independent accumulators or actual threading). **Next: pack router weights in the real HP `Lin` format and run through `lin_mm_hp` for a true apples-to-apples comparison** (see the follow-up row below). |
| 2026-07-26 | Router-as-HP-Lin, real (pooled, multi-threaded) | 4822d68 | Followed the retraction above: router weights packed via `lin_new_hp` (real vendor int4 format, same as every other `Lin`), computed via `lin_mm_hp` (pooled, multi-threaded, the proven ~446ns/call kernel from A3) — a true apples-to-apples test, not the earlier single-threaded hand-written fp16 dot product. `ly->router` (fp32) kept only as the validation reference. **First validation run: 1344/1344 (100%) expert-set mismatches vs fp32** — a real bug, not a quantization surprise: the top-8 selection loop's argmax sentinel (`bv=-1`) is only safe for post-softmax probabilities (always positive); the validation's `selr` was computed from **raw** fp32 logits, which run well below -1 (observed range roughly -1.7 to -5 for top candidates), so the sentinel silently rejected valid negative candidates. Fixed (`bv=-1e30f`). Also found and fixed a *separate* measurement-boundary bug of the same kind as the earlier `lin_mm` instrumentation gap: had moved the `gT_router` stop-timer to right after `lin_mm_hp`, but the original baseline's `router` bucket also bundled in softmax+top-8-select+renormalize — moved the stop-timer back to match exactly, so before/after numbers are genuinely comparable. | correctness + tok/s | **After the sentinel fix: 791/1344 (58.9%) expert-set mismatches remain, avg 1.38/8 experts differ per mismatch** (added this finer metric — "differs" is usually one near-tie swap, not wholesale reshuffling; max abs/rel deltas, 8.45 / 403%, are outlier-dominated across 172032 individual logit comparisons and not representative of the typical case). Generation still coherent on this short test (`' Tokyo'` PASS, 28 tokens). **Clean timing: router 18.7ms→12.5ms (-33%), wall 132.6ms→128.5ms (-3%), 7.51-7.54→7.78 tok/s (+3-4%)** — real but modest, not the earlier (buggy) ~40x reading. **Does not cleanly meet "keep only if quality holds and the bucket falls materially"**: speedup is real but modest; routing quality does not cleanly hold (majority of decisions perturbed, though usually by one near-tie expert). Open decision, not resolved here: revert to fp32 router, keep as-is, or investigate whether swapped experts typically have low renormalized weight (→ low output impact) before deciding — needs broader eval (longer generations, more prompts, ideally perplexity) to actually answer, not another short coherence check. **Caveat on the earlier fp16 experiment's "0/1344 mismatches" result**: that test used the same vulnerable sentinel pattern; given fp16's much smaller rounding error the result is still plausibly correct, but it was not verified against this bug and the code no longer exists to re-check. |
| 2026-07-26 | Vendor int8 M1 kernel — validated, not yet wired to production | 232144d | Per review item 4 ("try a vendor-style W8 HP router... could retain matrix-engine/pool speed while reducing routing error"): `gemm_kernel_i8i8_m1` (ime2_kernels.cpp:4773) is real and dispatched for count_m<4, same pattern as the int4 kernel. Genuinely simpler than int4: plain typed `vmadot ...,i8` (signed x signed, **no zero-point trickery at all** — int8 has enough range to store signed values directly, unlike int4's unsigned-nibble+implicit-zp=8 hack), and pairs with the **simple** A-format (fp32 scale + int16 asum + 32B int8, 38B/group) that the *first* (wrong) int4 attempt assumed — that format wasn't wrong, it just belonged to a different kernel. B-pack ground truth from `make_block_q8_0x32` (repack.cpp:357): plain row-major `memcpy` per row, no interleaving needed (int8 doesn't need int4's nibble-pairing trick). Ported+validated standalone (`bench/vendor_ime_i8_probe.c` for toolchain, `bench/vendor_ime_i8_full.c` for correctness+timing) before touching production, same discipline as A1-A2. | correctness + timing | **Max abs diff 0.00000, max rel diff 0.00001 — essentially bit-exact against the dequant oracle, first try, no reordering bug this time** (byte layout was simple enough to get right from ground truth alone). **Hot kernel-only timing: 791.7 ns/call vs int4's 446.4ns/call (A3) — ~1.77x slower per call**, consistent with streaming 2x the weight bytes (int8 vs int4 density). Still uses the same pooled/multi-threaded dispatch infrastructure as int4, so expect real speedup over the single-threaded fp32 baseline (18.7ms), likely landing between int4's 12.5ms and fp32's 18.7ms — needs the real per-token bucket measurement once wired into `qwen_moe_hp.c` as a third router mode alongside `fp32` (default) and `g_router_use_hp` (int4, experimental). Not yet wired to production or validated for actual routing-quality impact on the real model — that's the next step. |
| 2026-07-26 | Vendor int8 M1 router — wired to production, real result on the actual model | fac7355 | Wired int8 M1 alongside int4-HP as a third `g_router_mode` (0=fp32 default, 1=int4, 2=int8), reusing the same pool infrastructure (generalized with a `kind` field) rather than duplicating dispatch code. One validate run now reports int4-vs-fp32 and int8-vs-fp32 independently, same 1344 comparisons (12 prefill + 16 decode x 48 layers), so the two tradeoffs are directly comparable from one pass. | correctness + tok/s + quality | **int8 is a materially better tradeoff than int4.** Quality: **105/1344 (7.8%) expert-set mismatches, avg 1.16/8 experts differ, max abs delta 0.576** — vs int4's 811/1344 (60.3%), avg 1.38/8, max abs delta 8.45 — roughly **8x fewer perturbed routing decisions and 15x smaller worst-case logit delta**. Speed: **router bucket 16.0ms** (int4: 12.5ms, fp32: ~18.7-21.9ms) — smaller speedup than int4 (~14% vs ~33% off the fp32 baseline) but real, and **7.69 tok/s** (int4: 7.78, fp32: 7.36-7.54). Generation stayed coherent (`' Tokyo'` PASS) in all modes on this 28-token test. **Still gated behind an experimental flag, not promoted to default** — 7.8% mismatch is much closer to "quality holds" than int4's 60.3%, but isn't clean zero, and the same caveat applies as before: a 28-token single-prompt test can't fully settle whether even this level of perturbation is truly benign. Worth a real decision now that both tradeoffs are quantified on the actual model, not left as another open item. |
| 2026-07-26 | Attention loop vectorized (RVV, reused router primitives) | 7dde26c | Per the standing review item ("otherwise move to activation packing or attention"), chose attention over activation-packing (its cost scales with context length). Reused the existing `vdot_f32` (RVV `vfmacc`+`vfredusum`, originally written for the router matvec) for the QK dot product, and added a new `vaxpy_f32` (`y[i]+=scale*x[i]`, RVV `vfmacc_vf`) for the AV weighted-sum accumulation — replacing the fully scalar triple-nested attention loop. Zero approximation risk (exact vectorization of the same math, same class of fix as the RoPE-cache and router-RVV changes, not a lossy approximation like SwiGLU would be). | tok/s + correctness | **attention 18.7ms→9.1-9.2ms (-51%), 7.51-7.54→7.68-7.89 tok/s.** Two independent runs, both `' Tokyo'` PASS with identical generation (Brasília, Ottawa): run 1 — attention 9.2ms, wall 130.3ms, 7.68 tok/s; run 2 — attention 9.1ms, wall 126.8ms, 7.89 tok/s (run-to-run noise, not a regression). **Bigger win than the router's RVV pass (1.6x)** despite reusing the same primitives — attention's QK/AV access pattern is dense and sequential (cache-friendly), unlike the router's ~1MB/layer memory-bandwidth-bound gather, so the same vectorization has more room to help here. Attention is no longer tied with router as the second-largest bucket; current ranking is linear(kernel) 59.0ms > router 22.1ms (fp32) > act-pack 17.4ms > swiglu 14.0ms > attention 9.1ms > rope 2.0ms > rest(other) 2.1ms. Activation-packing remains the other, still-untouched half of this review item. |
| 2026-07-26 | Activation-packing vectorized (RVV port of the real vendor `quantize_a_row_i8_hp`) | 1d10648 | Closed out the other half of the review item. Traced the real vendor RVV source (`rvv_kernels.cpp:1989`, `vlenb==128` branch — this board's A100 cores, VLEN=1024) instead of hand-writing a vectorized quantizer from scratch, same discipline as the M1/int8 kernel ports. Ported verbatim (`bench/vendor_ime_actpack_probe.c`), validated against the shipping scalar `pack_A_hp` across 200k random trials spanning 5 distributions (generic, tiny-magnitude, outlier, all-zero, alternating-sign). Found and fixed one real edge-case divergence (all-zero rows: the scalar port's defensive `1e-6f` amax floor vs. the vendor code's bare `0.0f` start — numerically inconsequential since a zero block contributes 0 to the matmul regardless of the stored scale, but added the floor to the RVV port anyway for a true byte-identical match, not just an equivalent one). Also hit and diagnosed a real infra gotcha along the way: a fresh probe binary reported `vlenb=32` (X100, wrong) even when pinned to hart 8 via `sched_setaffinity` alone — turned out `bind_ai()` (write `"0"` to `/proc/set_ai_thread`, called by `qwen_moe_hp.c`'s `main()` before its own pinning) is what actually grants a thread access to harts 8-15 under this session's cgroup (`Cpus_allowed` capped to `0-7` at the shell level); `sched_setaffinity(CPU_SET(8))` alone silently no-ops without it. | correctness + timing + tok/s | **Byte-identical across all 200k trials after the floor fix (0 mismatches, 0 byte diffs).** Hot kernel-only timing: 6009.7ns/call scalar vs 1542.4ns/call RVV — **3.90x**. Wired into production (`pack_A_hp` in `qwen_moe_hp.c`, no feature flag needed — byte-identical, not approximate, same as the attention change). **Real result: act-pack 17.4ms→2.3ms (-87%, more than the isolated hot-kernel ratio predicted — likely fewer scalar loads/branches reducing cold-cache cost too, not just per-call throughput), 7.68-7.89→8.84-9.25 tok/s** — a bigger single win than the attention change. Two runs, both `' Tokyo'` PASS, identical generation (Brasília, Ottawa, Canberra, Cairo, New Delhi on the longer run). Current bucket ranking: linear(kernel) 58.6-58.7ms (dominant, ~52% of wall) > router 18.3-18.9ms (fp32) > swiglu 14.0ms > attention 9.2-14.7ms (grows with context) > act-pack 2.3ms > rope 2.0ms > rest(other) 2.1ms — act-pack, once tied with swiglu as the third-largest bucket, is now among the smallest. |
| 2026-07-26 | Linear subclass instrumentation (qkv / o / expert / lm_head) | d526736 | Measure-first follow-up after act-pack closed: partition the 58.6ms `linear(kernel)` parent bucket by consumer class via `g_lin_class` + `lin_add()` (does not change math). Same board/path: nt=4, fp32 router, HP cache. | ms/tok breakdown | **9.24 tok/s, `' Tokyo'` PASS.** Parent linear still 58.6ms; breakdown **expert 35.9 (61%) > qkv 9.4 (16%) > o 7.6 (13%) > lm_head 5.7 (10%)**, subclass sum matches parent. Expert FFN (8× gate/up/down) is the clear majority of residual linear time — next optimization target; QKV/O micro-opts are second-order. |
| 2026-07-26 | Is `linear(kernel)` actually memory-bandwidth-bound? Raw ceiling probe says mostly no | d526736 | Before touching the hottest, most-validated kernel in the file, checked the standing "memory-bandwidth-bound" framing (used throughout this session to explain the router's 1.6x RVV ceiling and nt=8's regression) against a real measurement. `bench/ram_bw_probe.c`: nt=4 threads (harts 8,10,12,14, same pinning as production) each stream-read a private 256MB buffer (far bigger than any cache), 4 passes, `bind_ai()`+pinned per the §infra-gotcha above. Compared against `linear(kernel)`'s per-token weight-byte volume (191.1MB/tok, computed from `BREC`×tile-count across q/k/v/o/8×experts) and against an nt=1 rerun of the real engine to isolate per-thread scaling. | GB/s | **Raw nt=4 concurrent sequential-read ceiling: 17.05 GB/s** (solo single-thread: 10.56 GB/s — the aggregate is only ~1.6x solo, not 4x, so even a clean streaming pattern doesn't scale linearly on this board past 1 thread, worth remembering). `linear(kernel)` achieves **3.26 GB/s effective (58.5ms @ nt=4) — only 19% of that ceiling.** Isolated the per-thread-scaling question with an nt=1 rerun: `linear(kernel)` 169.8ms (**1.13 GB/s**, close to A3's original 446.4ns/call/576B≈1.29GB/s isolated-kernel rate — consistent, not a new number) → nt=4's 58.5ms is only a **2.90x speedup over nt=1, not 4x**, despite nt=4 giving one thread per physical IME-2 unit (no unit-sharing, unlike nt=8 which puts 2 harts on each of the 4 shared units — the much better-understood cause of *that* regression). **Conclusion: DRAM-controller bandwidth is not close to saturated (81% of the streaming-read ceiling is idle) — the sub-linear nt=1→nt=4 scaling has a different, still-unidentified cause** (candidates: pool-dispatch/sync overhead accumulating over ~1392 small Lin calls/token, real per-call access being far less favorable than a clean 256MB sequential stream since each Lin's weight buffer is a separate small heap allocation — no row-buffer/prefetcher continuity between calls, or the kernel's own fp16-accumulation compute path being the true per-thread ceiling rather than memory fetch). **This revises, not confirms, the "memory-bandwidth-bound" framing used to justify not pursuing further kernel work** — there may be real headroom, but the next step has to isolate dispatch overhead from kernel-compute time before guessing at a fix, not another kernel rewrite on faith. |
| 2026-07-26 | RETRACTED (external review): the row above has a factor-of-8 byte-count error | — | `tiles(N,K)=(N/32)*(K/256)` correctly counts N32×K256 *superblocks*, but was multiplied by `BREC` (576B, one 32-wide-K sub-record) instead of `BSUPER` (`NSUB*BREC`=4608B, the actual bytes in one full superblock — `NSUB=8` sub-records each). Present identically in both the nt=4 and nt=1 figures above, so it canceled out of their *ratio* (the "2.90x not 4x" scaling claim is unaffected) but invalidated every absolute GB/s number. | GB/s (corrected) | **Corrected: 1.529 GB/token** (was wrongly 191.1MB — matches this doc's own much-earlier "Honest perf model" estimate of ~1.5-1.9GB/token). **nt=4 → 26.13 GB/s** (was wrongly "3.26 GB/s, 19%"), **nt=1 → 9.00 GB/s** (was wrongly "1.13 GB/s") — nt=1's corrected rate now closely matches A3's single-call rate reinterpreted correctly (446.4ns for one full 4608B call ≈ 10.32 GB/s). `bench/ram_bw_probe.c`'s "17.05 GB/s ceiling" is also not trustworthy on its own terms — a scalar `volatile` dependent load is latency/outstanding-request-limited, not representative of the kernel's wide vector loads; tellingly, the corrected 26.13 GB/s *exceeds* that supposed ceiling. **Reverts, doesn't confirm, the retraction above** — the evidence for "DRAM is 81% idle" is gone, but that doesn't by itself prove bandwidth IS the limiter either; see the next row for the actual isolation experiments. |
| 2026-07-26 | Five follow-up probes isolating the real cause, one at a time — closed out, one actionable finding | 3f1b67c | After the byte-count fix, ran five probes in sequence, each removing one variable at a time: (1) `bench/run_hp_m1_scaling_probe.c` — raw kernel, no dispatch, small reused (cache-hot) private buffers; (2) `bench/pool_dispatch_overhead_probe.c` — same hot buffers, routed through a faithful replica of the real persistent spin-dispatch pool at production's real per-round granularity; (3) `bench/cold_streaming_probe.c` — real cold (≥256MB/thread, single-pass, no reuse) working set, 4 variants (contiguous / production Lin sizes+order / randomized order / `MADV_HUGEPAGE`); (4) `bench/shared_buffer_scheduling_probe.c` — same cold working set but genuinely *shared* across threads through the real pool, comparing production's real cyclic panel stride (`np=tn;np+=nt`) against blocked (contiguous per-thread panel range), plus a real per-panel-unique output write vs a reused scratch buffer; (5) `lm_head` isolated on its own (`Np=4748`, ~40x the next-largest Lin tested) under the same cyclic-vs-blocked comparison. | GB/s, scaling ratio | **(1) 10.54→41.13 GB/s, 3.90x — kernel/tensor-unit contention ruled out.** **(2) 10.23→39.49 GB/s, 3.86x, within ~4% of (1) — dispatch overhead ruled out.** **(3) cold access alone reaches 30-36 GB/s at nt=4 across all 4 variants; randomized-vs-ordered and hugepage-vs-normal both show no real difference — call-order predictability and TLB/page-size pressure ruled out.** **(4) cyclic 29.34 vs blocked 29.09 GB/s across q/k/v/o/experts — no meaningful difference; real output writes 28.95 vs 28.76 GB/s — also no difference. Both ruled out for every Lin except one.** **(5) `lm_head` alone: cyclic 26.18 GB/s vs blocked 29.45 GB/s — a real ~12% gap, and cyclic's number lands almost exactly on production's measured 26.13 GB/s overall rate.** Phrased cautiously per review: this demonstrates a real *scheduling* effect (cyclic gives each hart an ~147KB stride between `lm_head` panels, repeated ~1187 times across a 175MB span, apparently defeating per-hart streaming/prefetch over that unusually long traversal), not a confirmed DRAM-row-buffer mechanism — that wasn't directly observed. **Stopping here, recorded as an unresolved ~10% delivery gap, not a new optimization target**: `lm_head` is only ~10% of `linear(kernel)`'s time, so even a full fix is worth roughly half a millisecond/token, not the whole 26→30GB/s gap; remaining untested candidates (real all-1344-buffers-at-once allocation pattern, A-buffer/pack interaction) are deliberately left untested as too small/poorly localized to justify further speculative probes. **One actionable, NOT YET APPLIED candidate**: switch `lin_mm_hp_worker_run` from cyclic to blocked panel assignment — for `lm_head` specifically, or as the new general default (blocked regressed nothing in probe (4)) — pending a real production A/B test. |
| 2026-07-26 | Blocked panel scheduling: production A/B — regressed, reverted, not adopted | 5723bef (docs only — the code change itself was reverted, never committed) | Ran the one remaining actionable candidate from the row above: `lin_mm_hp_worker_run` cyclic→blocked, both int4-HP and int8-M1 paths (panels are independent — disjoint `y[np*32..]` writes — so this is a pure scheduling change, no oracle validation needed). Two binaries from the same tree (cyclic=committed HEAD, blocked=one-line change), same prompt/cache/fp32-router/nt=4/generation length, run back to back on the board twice. | tok/s + bucket breakdown + correctness | **Tokens identical every run (`' Tokyo'` PASS, matching generation) — correctness as expected.** **Throughput was the OPPOSITE of the isolated probe's prediction, reproducibly**: `qkv` 9.4→10.2ms, `o` 7.6→8.5ms, **`lm_head` 5.7→6.0ms** (worse, not the predicted ~5.1ms) — identical both runs; only `expert` improved slightly (36.0→~35ms); net `linear(kernel)` worse both runs (+1.0-1.7%). **Failed the pre-registered keep criterion ("keep only if `lm_head` falls toward ~5.1ms with no regression elsewhere") on both counts.** **Reverted immediately — `qwen_moe_hp.c` is back to the exact prior committed state, no production code change lands from this experiment.** Root cause of the mismatch between the isolated probe and production not diagnosed and, per the standing decision to close this investigation, not being chased further — this also means probe (4)'s "no difference for `qkv`/`o`" finding didn't transfer to production either, not just probe (5)'s `lm_head` prediction. General lesson: an isolated synthetic probe is real evidence for ruling a factor in or out, but a production A/B is still required before adopting anything derived from it. See `codex_recs_1.md` §22.14. `linear(kernel)` work is closed for this session; next high-value branch is a multi-prompt quality harness (unlocks safely evaluating SwiGLU approximation and the int8 router past "experimental"). |
| 2026-07-26 | Multi-prompt quality harness built; int8-M1 router promoted to default | 39ecd78 | Replaced single-prompt `' Tokyo'`-coherence with teacher-forced evaluation across 10 fixed prompts (2 each: factual, reasoning, code, multilingual — French+Chinese, long-context ≥113-tok prefill), tokenized via the real tokenizer (`llama-tokenize --ids --no-bos`, cross-checked against the known-good golden prompt array). Per-prompt: generate a reference continuation greedily under fp32; replay the same tokens under int8 via teacher forcing (feed the reference token regardless of int8's own choice, so divergence never compounds); compare `argmax(int8_logits)` vs reference and compute NLL of the reference token under int8. Promotion thresholds fixed in code before any run: router mismatch <10%, avg NLL delta <0.5 nats/tok, token divergence <15%, router bucket >=10% faster. Found and fixed three real bugs building this (none in the eventual harness logic): (1) a genuine pre-existing memory-corruption bug, newly exposed not caused — an 8th positional CLI arg (`argv[7]`) read correctly at `main()` entry but was reproducibly clobbered by the time execution reached `lin_mm_pool_init()` (confirmed via `dmesg`'s `badaddr` matching exactly); never manifested before since nothing previously read past `argv[6]`; **not root-caused**, worked around via `QWEN_HARNESS=1` env var instead — flagged here for whoever next touches `cache_load`/`model_load`. (2) The file's own documented "vmadot-adjacent autovectorization" toolchain gotcha, hit a third time — new plain-scalar harness functions (no custom instructions at all) caused the *unconditional* baseline decode path to segfault; fixed with the established `noinline,optimize("no-tree-vectorize")` mitigation. (3) A measurement-contamination bug matching this session's "always suspect the measurement before the hardware" pattern — `g_router_validate=1` left on during the timed int8 pass added uninstrumented per-step overhead that landed in the untimed `rest` bucket (1.4ms→38.1ms/tok), producing a bogus "int8 router is 29% slower" result; fixed by splitting into an untimed validate pass (router-mismatch stats) and a clean validate-off timed pass (speed). | quality thresholds + speed + correctness | **All four thresholds PASS: router expert-set mismatch 6.1% (1557/25392, <10%), avg NLL delta -0.0034 nats/tok (<0.5), token-argmax divergence 2.1% (4/192, <15%), router bucket 13.7% faster (18.7→16.1ms, >=10%).** The corrected speed number closely matches this session's independently-measured §22.8 figure (~16.0ms int8 vs ~18.3-18.9ms fp32), confirming the fix rather than just flipping the sign to what was expected. 6.1% mismatch is notably better than §22.8's single-prompt 7.8% estimate now measured across real prompt diversity (25392 comparisons vs 1344). Only 2 of 10 prompts (multilingual/French, long-context/narrative) showed any token divergence at all. **int8-M1 router promoted to production default** (`g_router_mode` default 0→2; `g_router_mode=0` kept as an explicit exact-fp32 revert flag; int4-HP stays available as `g_router_mode=1` but remains rejected per §22.7). Verified post-change on the board: default invocation reports `router=int8-M1(default)`, `' Tokyo'` PASS, router bucket 16.2ms. SwiGLU approximation evaluation is the explicit next step, not started — the harness is general enough to reuse unchanged once a candidate exists. |
| 2026-07-26 | Memory corruption root-caused (same bug as autovec gotcha); global `-fno-tree-vectorize` adopted | 1c6b064 | Per explicit direction after the int8 promotion: memory-corruption/toolchain hardening before SwiGLU. Rebuilt a repro (§22.15's `argv[7]` corruption, restored just for this test) three ways to isolate cause: `-fsanitize=address -O1`, current `-O3` source, pre-`noinline`-fix `-O3` source. Also bisected the *now-default* int8-M1 router path's real remaining slowness by testing `pack_A_i8` and `lin_mm_hp_worker_run` individually with the `noinline,optimize("no-tree-vectorize")` attribute, then testing a global `-fno-tree-vectorize` build flag for comparison. | correctness + speed | **`argv[7]` corrupted only in the pre-fix build — the harness's §22.15 `noinline` fixes had already resolved it; "not root-caused" was written before retesting.** One bug (vmadot-adjacent autovectorization collision, documented 2x already in `PROGRESS.md`), two symptoms (SIGSEGV and silent corruption), not two bugs. Separately: `pack_A_i8` alone 16.2ms→15.1ms, `lin_mm_hp_worker_run` alone →15.6ms — each a real but partial contributor; **global `-fno-tree-vectorize` captured the full effect: router 16.2ms→4.4ms (int8 default), and — via functions never individually tested — also improved the untouched fp32 router path (~18-19ms→11.1ms), proving real uncaught exposure remained.** Net decode **8.84-9.25→9.5-9.9 tok/s**, `' Tokyo'` PASS unchanged, verified across all three router modes with repeat runs. Adopted as the standing build flag (`qwen_moe_hp.c` header comment) — every real hot path here is already explicitly vectorized (RVV intrinsics/asm), so gcc's auto-vectorizer was never buying performance, only risk. Per-function attributes kept in place (harmless, documents specifically-confirmed-affected functions) but are no longer the load-bearing fix. |
| 2026-07-26 | Quality harness re-run under mandatory `-fno-tree-vectorize` build — confirmed, new baseline | ecb2e68 | Per explicit instruction, re-ran §22.15's identical 10-prompt harness before starting any SwiGLU work, built with the exact mandatory command now in `qwen_moe_hp.c`'s header (§22.16). | quality thresholds + speed + correctness | **All four thresholds PASS again: router mismatch 6.1% (1541/25392, <10%), avg NLL delta +0.0060 (<0.5), token divergence 1.6% (3/192, <15%), router speedup 57.2% (10.2→4.4ms, >=10%).** Quality metrics essentially unchanged from §22.15 (expected — the flag only affects gcc's autovectorizer, never explicit RVV intrinsics/asm, so no numeric difference should exist and none does beyond ordinary run-to-run noise). The larger speed delta here (57.2% vs §22.15's 13.7%) reflects both fp32's and int8's absolute router-bucket costs dropping under the flag, with int8 dropping proportionally further — consistent with §22.16's own standalone-decode table, not a new/different result. **This run supersedes §22.15's as the reference baseline** for judging any future router or SwiGLU change. Next: implement one flagged fast-SwiGLU candidate with quality/speed gates predeclared before implementation, per explicit instruction gated on this confirmation. |
| 2026-07-26 | Fast-SwiGLU (hard-swish) implemented with predeclared gates, promoted to default | 7313e4f | Gates fixed before implementation: avg NLL delta <0.3 nats/tok (stricter than router's 0.5 — SwiGLU runs on every expert every layer, not once/layer like routing), token divergence <15% (same as router), swiglu bucket >=15% faster (stricter than router's 10%). Candidate: hard-swish (Howard et al. 2019, MobileNetV3) — `x*clamp((x+3)/6,0,1)`, zero transcendentals, pure RVV vector ops (no reliance on gcc autovec, consistent with §22.16). Validated the RVV implementation against a scalar reference of the *same* formula first (`bench/swiglu_hswish_probe.c`: 0/38.4M element mismatches, max abs diff 3.8e-6) — checks vectorization correctness only, not approximation quality (no bit-exact oracle exists for a deliberate approximation, unlike every other RVV port in this file). Extended the quality harness with a new phase: reference held at *today's* production default (int8 router + exact SwiGLU, not the original fp32 ground truth) since the question is whether adding this on top of what already ships causes a problem, not a re-litigation of the router decision. | NLL delta + divergence + speed + correctness | **All three thresholds PASS: avg NLL delta +0.0969 (<0.3), token divergence 5.7% (11/192, <15%), swiglu bucket 96.2% faster (14.00→0.53ms, >=15%).** Both quality numbers are meaningfully higher than the router's corresponding figures (NLL delta ~10-30x larger, divergence ~3x larger) — expected and anticipated when the stricter thresholds were set: a real approximation of a nonlinearity is a bigger quality gamble than a numerically-close weight quantization. Clears the bar comfortably but by a smaller margin than the router did. Speedup is dramatic — removes per-element `expf()` calls entirely across 768 elements × 8 experts × 48 layers × every token, previously the single most expensive per-element scalar op after activation-packing was fixed (§22.10). **Promoted to production default** (`g_swiglu_fast` 0→1) per the standing "keep both behind flags until they pass the harness" instruction; `g_swiglu_fast=0` kept as an explicit exact-SiLU revert flag. Verified on the board, repeated runs: `' Tokyo'` PASS, identical generation, swiglu 14.0ms→0.4-0.6ms, **decode 9.5-9.9→11.49-11.5 tok/s** — the largest single per-token-time win of the session. **Session cumulative from the 1.49 tok/s baseline: ~7.7x**, within ~2% of the real vendor binary's 11.71-12.89 tok/s. |
| 2026-07-26 | RETRACTED: hard-swish SwiGLU promotion was premature — default reverted to exact SiLU | b94985a | Same-day external review of the row above. The promotion cleared its predeclared thresholds but the thresholds themselves, and how the result was reported, didn't surface three real problems: (1) `exp(0.0969)≈1.102` — the "+0.0969 nats/tok, PASS" NLL delta is actually a ~10.2% perplexity inflation, obscured by reporting raw nats instead of the perplexity-multiplier framing; (2) 192 total generated tokens is two orders of magnitude less evidence than the router's 25,392 comparisons, thin for a change this consequential; (3) **hard-swish is not a numerically-close approximation of SiLU, it's a different activation function** — MobileNetV3 was *trained* with hard-swish (weights co-adapted to its piecewise-linear shape); Qwen3-30B-A3B was trained with exact SiLU and has never seen hard-swish's shape, so applying it post-hoc is a real distribution shift on an unadapted model, categorically different from every other exact-or-bounded-quantization technique promoted this session. | correctness of the promotion decision itself | **`g_swiglu_fast` default reverted 1→0 (exact SiLU); `g_swiglu_fast=1` (hard-swish) remains available as an explicit experimental flag, not removed** — the RVV implementation's validated correctness (0/38.4M mismatches vs the scalar hard-swish formula, row above) is unaffected, only the promotion is retracted. Also fixed three documentation phrasing issues from the row above: "96.2% faster" was ambiguous (now stated as "96.2% time reduction / ~26.4x faster"); PROGRESS.md's prose mistakenly said "(<15% required)" for the speed gate when both the code and the intent were "(≥15% required)"; "within ~2% of 11.71-12.89 tok/s" compared only the vendor binary's favorable low endpoint (11.49/11.71≈98.1%) while omitting that it's ~11% below the high endpoint (11.49/12.89≈89.1%) — both endpoints now stated. **Remediation plan before any re-promotion is reconsidered** (`codex_recs_1.md` §22.19): expand evaluation to several thousand tokens spanning perplexity-style evaluation on real (not model-generated) text, code, multilingual, reasoning, and free-running (not only teacher-forced) generation; compare the full combined production stack against the original fp32-router+exact-SiLU baseline, not only pairwise against the immediately-prior config; try a genuine polynomial/rational sigmoid approximation (approximating the actual trained-against function, not substituting a different one) which may retain most of the ~13.5ms bucket gain with materially lower NLL drift; re-threshold in perplexity-multiplier terms rather than raw nats. None of this started yet. |
| 2026-07-26 | Rational-SiLU candidate evaluated on board — PASSES all gates, hard-swish RE-CONFIRMED rejected, neither promoted | fe7d7c1 | Added `g_swiglu_fast=2`, an RVV rational-Padé approximation to the actual sigmoid, plus `bench/swiglu_ratsig_probe.c` (0/38.4M mismatches, 2.51-5.18x lower mean-abs-error than hard-swish vs true SiLU, RNG bug fixed to sample `[0,1)` before accepting results). Expanded harness: 376 teacher-forced generated positions, 687 independently authored real-text perplexity positions, full-stack comparison against fp32-router+exact-SiLU, free-running spot checks. | teacher-forced ppl mult / divergence / speedup / real-text ppl mult | **hard-swish: x1.1101 vs production (11.0% inflation) — FAILS the <1.05x gate** (6.1% divergence, 24.4x speedup); confirms §22.19's retraction under the larger sample. **Rational-Padé: x1.0201 vs production (2.0% inflation) — PASSES** (3.2% divergence, 10.0x speedup/90.0% bucket reduction; x1.0262 vs original fp32+exact). Real-text (687 tok): both candidates clear the aggregate gate (hard-swish 0.9915x, rational-Padé 0.9841x) *and* every individual corpus (<1.10x) — real-text alone would have wrongly cleared hard-swish, so the teacher-forced check remains decisive. Free-running spot check corroborates: rational-Padé tracks production's phrasing, hard-swish diverges narratively. **No promotion**: `g_swiglu_fast` default stays `0`. Rational-Padé (`=2`) has passed every harness gate but a production A/B (§22.14 precedent) hasn't been run yet — promotion is left to explicit confirmation. See `codex_recs_1.md` §22.20. |
| 2026-07-26 | Rational-Padé production A/B — CONFIRMED, promoted to default | 540abb1 | One bounded A/B per explicit scope: identical build/cache/canonical-prompt/router(int8-M1)/nt=4/ngen=16, varying only `g_swiglu_fast` (0 vs 2), two paired trials run interleaved (exact, ratsig, exact, ratsig) to control for drift, same discipline as the §22.14 scheduling A/B. | tok/s, wall time, all per-token buckets, token identity | **Exact SiLU: 9.55 & 9.92 tok/s (avg 9.735, swiglu bucket avg 15.85ms). Rational-Padé: 11.38 & 11.37 tok/s (avg 11.375, swiglu bucket avg 1.25ms).** SwiGLU bucket reduction **92.1%** (exceeds the expected ~90%); wall time -14.4% (14.8ms/token saved); tok/s **+16.8%, landing at 11.35-11.38** (matches the 11.0-11.4 tok/s prediction). Every other bucket (act-pack, linear-kernel, attention, rope+qknorm, router, rest, qkv/o/expert/lm_head breakdown) statistically unchanged across all four runs (≤0.3ms deltas) — no regression elsewhere, the pre-registered keep-criterion. All four runs `' Tokyo'` PASS, generated tokens byte-identical between configs on the canonical prompt in every trial. **Promoted: `g_swiglu_fast` production default `0→2`** (rational-Padé); `g_swiglu_fast=0` (exact SiLU) kept as an explicit revert flag; `g_swiglu_fast=1` (hard-swish) stays rejected. Verified the promotion holds with *no* CLI arg at all (real production invocation): `swiglu 1.4ms`, `11.35 tok/s`. Session cumulative from the 1.49 tok/s baseline at the actual current default: **~7.6-7.8x**, now 97.1%/88.2% of the vendor binary's `nt=4`/`nt=8` endpoints (both stated). See `codex_recs_1.md` §22.21. Stale router/SwiGLU-default claims elsewhere in this file (§13) and in `qwen_moe_hp.c`'s source comments were also corrected in this pass. |
| 2026-07-26 | Release-quality checkpoint frozen | 0dde2f4 | Per explicit direction, recorded compiler/build command/artifact hashes for the promoted state (int8-M1 router + rational-Padé SwiGLU). | reproducibility | gcc 15.2.0 (Bianbu), Binutils 2.46, `-O3 -fno-tree-vectorize -march=rv64gcv_zvfh_xsmtvdotii -fopenmp`. sha256: source `3b9bc183b6...`, binary `c28c1f66e2...`, model gguf `8b8e9febb6...`, HP cache (v2) `f9da3b65e5...`. Source hash matches `git show 0dde2f4:qwen_moe_hp.c` exactly — the frozen board binary is provably built from the committed promotion commit. Re-verified: `' Tokyo'` PASS, 11.35 tok/s. Full table in `codex_recs_1.md` §22.22. |
| 2026-07-26 | Context-length scaling swept — attention/linear crossover found | dcee848 | New `QWEN_CTXLEN` env var synthesizes a prefill of a requested length (tiling the harness's 113-token narrative prompt) and grows the KV cache to fit, same production binary/build. Swept ctx ∈ {32,64,128,512,1024} at the promoted default config, nt=4. | per-token bucket vs context length | `linear(kernel)` flat (58.8-60.7ms) across nearly 2 orders of magnitude of context; `attention` scales linearly, ~1.197ms/token of context (least-squares fit over 32-1024), intercept ≈-11ms. **Crossover (attention overtakes linear as dominant bucket): ~59 tokens of context** — directly bracketed by the ctx=64 measurement (attn 64.4ms > linear 58.9ms). All of this session's router/SwiGLU decisions were measured at a 12-token prompt, well inside the linear-dominated regime; those speedups touch only ~5% of wall time by ctx=1024. tok/s falls from 11.35 (ctx=12) to 0.77 (ctx=1024) as attention grows; prefill also grows steeply (0.97s→680s) but wasn't the metric under test. Flags attention (RVV QK/AV dot products, no pruning/windowing) as the natural next target for long-context throughput — not started. Full table in `codex_recs_1.md` §22.23. |
| 2026-07-26 | Board OOM incident (root-caused) + vendor `llama-bench` bug found+fixed + clean vendor A/B — AT PARITY | n/a (operational, no code changes) | Running the expanded harness and `llama-bench` concurrently (sequencing mistake) exceeded the board's 32.8GB RAM / 0 swap; `journalctl` confirms the kernel OOM-killer killed the harness process and took sshd down with it (~15-20min outage, banner-exchange timeouts). Temps 57-61°C throughout, not the cause. Separately, `llama-bench` then crashed even run alone (`ggml_abort`, thread-affinity error) — traced to `LD_LIBRARY_PATH=/usr/lib` loading a stale (Jul 7) system `libggml-cpu.so` that predates an already-applied local source patch; the build tree's own copy (Jul 25) has the fix. | tok/s (clean, no contention) at nt=4 | Fix: `LD_LIBRARY_PATH=/root/llama.cpp/build/bin` instead of `/usr/lib`, no source/system changes. **Our engine 11.35-11.39 tok/s vs vendor 11.38±0.21 tok/s at nt=4 — at parity**, within the vendor's own noise band (nt=8 vendor: 12.82±0.05, close to the earlier stale-library baseline's 12.89, confirming the fix changed only the crash, not the underlying number). One honest caveat: our decode window is KV positions 12-27 (after a 12-token prefill) vs vendor's `-p 0` empty-context positions 0-15 — a slightly easier attention profile for vendor, though the results already land within noise of each other so it doesn't change the parity conclusion. Lesson (also saved to Claude's persistent memory): never run two model-loading processes on this board concurrently. Full writeup in `codex_recs_1.md` §22.24. |
| 2026-07-26 | Expanded harness crash root-caused with ASan — 5 real bugs found (1 pre-existing OOB read, 4 pre-existing prompt-truncation bugs), fixed, clean ASan rerun confirms every conclusion | 17513d1 | Rerunning the corpus/generation-length expansion alone (no contention) crashed (SIGSEGV, heap corruption). Rebuilt with `-fsanitize=address` (same methodology as §22.16) and reproduced under instrumentation. | memory-safety root-cause | ASan: **global-buffer-overflow READ** in `harness_eval_ppl`, `ppl_reason` declared length 176 vs real array size 149 (stale, predates this session's corpus expansion) — every real-text-perplexity run including it this session read 27 tokens past the array into `ppl_code`'s memory. Auditing every other declared length against actual array size (proactive, not just ASan-reactive) found 4 more, all pre-existing: `hp3`(15→16), `hp4`(22→24), `hp9`(113→124), `hp10`(113→155, **42 tokens dropped, over a quarter of the intended long-context prompt**) — under-declared, not memory-unsafe, but silently truncating the prefill every harness run this session actually used. All 5 fixed (metadata corrected to match real array content, which was verified sane by inspection first); `HARNESS_MAXCTX` also raised 200→512 (computed properly) and a hard bounds guard added directly in `forward()`'s KV-cache write. **Second ASan run, after all fixes, completed clean end-to-end** — full harness finished naturally, no report. Corrected results (760 teacher-forced / 2182 real-text positions) confirm every prior conclusion unchanged: hard-swish still FAILS (13.4% inflation vs production), rational-Padé still PASSES (1.7%/0.5% inflation), router re-confirmed (2.8% divergence, 87.0% faster) — no promotion status changes, since rational-Padé was promoted on §22.21's production A/B, not this harness. Full writeup in `codex_recs_1.md` §22.25. |
| 2026-07-26 | Fresh production A/B (exact vs rational-Padé) reconfirms §22.21; found+fixed a real production-path KV-sizing bug | 5cdb721 | Reran §22.21's exact A/B methodology fresh given everything since (OOM incident, vendor lib fix, harness bugfixes): identical build/cache/prompt/router/nt=4/ngen=16, 2 paired trials. Then extended the qualitative check to `ngen=60` to test output quality beyond the canonical window. | tok/s, wall time, buckets, token identity, output quality at longer ngen | **92.2% SwiGLU bucket reduction, wall -14.5%, tok/s +16.9%** (9.71→11.355 tok/s avg) — reproduces §22.21 (92.1%/-14.4%/+16.8%) almost exactly; every other bucket flat, tokens byte-identical in all 4 trials. **`ngen=60` immediately tripped the new forward() guard** (`KV position overflow: pos=64 ctx=64`): `main()`'s ctx sizing only grew for `ngen` under `QWEN_CTXLEN`, leaving the default-prompt path's ctx hardcoded at 64 regardless of `ngen` — any production invocation requesting >~52 generated tokens would have silently overrun the KV cache, live in the shipped binary all session, not harness-only. Fixed (ctx now sized from whichever prefill length is actually in effect); reverified clean at ngen=60 under both configs, default ngen=16 unaffected. At ngen=60 (context 12→71, past the §22.23 crossover) SwiGLU's savings barely move tok/s (8.82 vs 8.88) — attention now dominates, a live confirmation of the crossover finding. Full writeup in `codex_recs_1.md` §22.26. |
| 2026-07-26 | Attention optimization Phase 1: QK/softmax/AV instrumentation + baseline scoreboard | 86ed6c0 | Per `attention_optimization_plan.md`'s execution directive, Phase 1 instrumentation only (no layout/threading/fusion). Split the existing `gT_attn` bucket into QK dot-product, softmax, and weighted-V accumulate sub-timers inside `forward()`'s per-head loop, no arithmetic changed. Verified byte-identical `' Tokyo'` output and negligible timing overhead (sub-buckets sum within ~0.1ms of the pre-existing bucket) before trusting any measurement. 2 trials/context at ctx=128/512/1024, `nt=4`, board otherwise idle. | QK/softmax/AV ms split vs context | **QK and AV are co-dominant, ~46-48% of the attention bucket each at every context tested — neither one alone is the bottleneck.** Softmax is secondary but non-trivial: 6.9% (ctx=128) down to 5.7% (ctx=1024). Both QK and AV scale close to linearly with context (128→512: ~4.3-4.4x for a 4x context increase; 512→1024: ~2.07-2.10x for a 2x increase), decomposing §22.23's aggregate ~1.2ms/token-of-context finding into its two real components. AV runs ~2.3-2.5% more expensive than QK at larger contexts, plausibly from `vaxpy_f32`'s read-modify-write accumulation vs `vdot_f32`'s single-scalar-write reduction — relevant to the planned Phase 4 fused-kernel work (both directions redundantly re-read the same K/V once per query head sharing a KV head, 8x redundant reads either way). Tokens byte-identical between both trials at every context. Full per-trial scoreboard in `attention_optimization_plan.md`'s own table; writeup in `codex_recs_1.md` §22.27. **No further phase started** — this baseline is the required review point before Phase 2 (head-major KV layout) begins. |
| 2026-07-26 | Attention optimization Phase 2: head-major KV layout — KEPT, dramatic gain | d7fbf6c | Explicit authorization: Phase 2 only, isolated head-major KV-layout A/B, not combined with threading or GQA fusion. Changed per-layer KV cache from `[position][kv_head][head_dim]` (2KB stride between positions for one head) to `[kv_head][position][head_dim]` (one head's history contiguous) — pure addressing change, same arithmetic/quantization. Validated in the plan's required order: ASan clean on a bounded `ctx=256/ngen=24` run, no KV bounds errors (guard silent throughout), token identity confirmed at the canonical prompt and every A/B context, then the A/B itself (2 trials/context, `nt=4`). Baseline preserved at `/tmp/qwen_moe_hp_kv_timemajor.c` (hash-verified identical to the exact binary the §22.27 Phase 1 baseline was measured against, so that data was reused rather than re-measured). | attention bucket / wall / tok/s vs context, short-context regression check | **Attention bucket: -58.8% (ctx=128), -65.6% (ctx=512), -68.5% (ctx=1024). tok/s: 4.63→7.39 (+59.6%), 1.50→3.56 (+136.9%), 0.77→2.16 (+179.9%).** Far beyond the plan's own modest "may improve prefetching" framing — the old layout's 2KB stride was hostile to hardware prefetching; a meaningful fraction of what looked like real attention compute cost was actually avoidable memory-layout overhead. Short-context (canonical prompt, 2 paired trials): wall time improved 0.85%, not regressed — both predeclared keep criteria (attention improves reproducibly, short-context doesn't regress >2%) cleared with large margins, every context pairing in tight trial-to-trial agreement. Bonus: prefill time also dropped substantially (ctx=512 ~172-185s→91s, ctx=1024 ~678-680s→279s). **KEPT — head-major is now the production KV-cache layout.** Threading (Phase 3) and GQA fusion (Phase 4) not started, per the explicit authorization scope; the co-dominant QK/AV baseline (§22.27) is read as confirming Phase 4 (exact GQA reuse) is the likely larger next opportunity, softmax (Phase 5) can wait. Full writeup in `codex_recs_1.md` §22.28. |
| 2026-07-26 | Attention optimization Phase 3: four-KV-group worker parallelism — KEPT, isolated from GQA fusion | 7b7b859 | Explicit authorization: Phase 3 only, isolated from GQA fusion; softmax deferred despite a small regression. Extended `HpWork` with a `kind==2` attention variant, reusing the existing persistent pool (no new threads) — worker `tn` handles KV heads `tn, tn+attn_nt, ...` and their query heads; private per-worker score scratch allocated once; dispatch/sync overhead measured directly (wall time minus busiest worker's own compute); QK/softmax/AV sub-timing kept thread-safe via per-worker indexed accumulators summed after each round. `g_attn_nt<=1` takes an entirely separate, byte-identical-to-Phase-2 code path. Validated in the plan's required order: tokens byte-identical at every worker count (1/2/4) and context tested; ASan clean at attn_nt=2 and attn_nt=4 on a bounded ctx=256/ngen=24 run; UBSan clean at attn_nt=4 on the same test — covering both halves of the "no ASan or UBSan failure" gate. | attention bucket / wall / tok/s vs Phase 2, short-context regression check | **Attention bucket: -75.0% (ctx=128), -75.0% (ctx=512), -74.4% (ctx=1024). tok/s: 7.39→10.77 (+45.7%), 3.56→7.73 (+117.1%), 2.16→5.625 (+160.4%).** Short-context (canonical prompt, 2 paired trials): wall time improved 8.25%, not regressed. All five predeclared keep criteria met with large margins — tokens identical, ASan+UBSan clean, attention-bucket reduction far past the 20% floor, tok/s improvement far past the 10% floor, no short-context regression. Dispatch/sync overhead stayed tiny throughout (0.09-1.14ms). **KEPT — four-KV-group worker parallelism is now the production attention path**, `g_attn_nt` defaulting to `min(nt,nkv)` (=4 at nt=4), `QWEN_ATTN_NT=1` as the explicit serial revert flag. The true production default invocation now reports `attn_nt=4` and **12.3-12.35 tok/s at the canonical prompt**, up from 11.3-11.5 before this entry — the short-context HEADLINE number itself moved. Softmax's relative share of the (now much smaller) bucket grew, but per explicit direction stays untouched. Also fixed a stale reference in the plan's own "Immediate next action" section (still said "Implement Phase 1" after Phase 1/2 completed) to correctly identify Phase 4 (GQA-fused kernels, eliminating 8x redundant K/V re-reads across query heads sharing a KV head) as the next identified opportunity, not started. Full writeup in `codex_recs_1.md` §22.29. |
| 2026-07-27 | Attention optimization Phase 4.1: multi-Q QK — KEPT, isolated from multi-Q AV | b0e5586 | Explicit authorization: Phase 4 only; multi-Q QK first; no AV fusion in same patch; `QWEN_QK_FUSE=0` as Phase 3 revert. Added `qk8_dot` (load each K chunk once, update 8 independent accumulators with vdot_f32-identical per-head FMA/reduce order); larger per-worker scratch `gpr×ctx`; integrated `QWEN_QK_VALIDATE` capture path. Validation: standalone probe bit-exact (11,496 cmp); production integration 60,426,240 cmp max_abs=0; ASan/UBSan clean at -O2 (UBSan -O1 hit a page-fault that did not reproduce at -O2/-O3, suspected but not confirmed compiler-related); tokens byte-identical fuse0↔fuse1 at every context. | attention / wall / tok/s vs Phase 3, short-context check | **Attention bucket: -9.8% (ctx=128), -9.0% (ctx=512), -10.3% (ctx=1024). tok/s: +1.1%, +3.2%, +6.1%. QK work alone -22–26%.** Short-context ~flat (-0.1% wall). Phase 3's 20%/10% floors not met (those were for threading); Phase 4 auth gate (reproducible attention/e2e + sanitize) met. **KEPT and promoted** — `g_qk_fuse` defaults to 1; free exact win, zero quality risk. AV still co-dominant and still 8×-redundant — Phase 4.2 is the residual larger opportunity. Softmax still deferred. Full writeup in `codex_recs_1.md` §22.30. |
| 2026-07-27 | Attention optimization Phase 4.2: multi-Q AV — KEPT, same discipline as QK | 9015555 | Explicit authorization: Phase 4.2 only, same end-to-end completion discipline used for QK. Added `av8_chunk` (load each V chunk once per position, update 8 independent output accumulators, chunk-outer/position-inner nesting, bit-exact to vaxpy_f32); split fused-QK branch into softmax-all-heads then switchable AV; `QWEN_AV_FUSE=0` as Phase 4.1 revert. Validation: standalone probe bit-exact (3,072 cmp); production integration 55,050,240 cmp max_abs=0; ASan-O2's default inlining hit a reproducible stack-use-after-scope in av8_chunk that vanished with -fno-inline at the same -O2 and did not reproduce in the isolated probe (isolated to a compiler inlining decision, not kernel logic); ASan-O1/O3 (production, 3/3) and UBSan-O2/O3 clean; tokens byte-identical fuse0↔fuse1 at every context. | attention / wall / tok/s vs Phase 4.1, short-context check | **Attention bucket: -31.5% (short), -35.1% (ctx=128), -33.7% (ctx=512), -34.6% (ctx=1024). tok/s: +4.8%, +13.2%, +21.6%. AV work alone -76–82%.** Roughly 3× Phase 4.1's own wall-time win (AV was the slightly larger co-dominant bucket). Short-context does not regress (slight improvement). Improvement/numerics/short-context criteria cleanly met; sanitizer criterion was not a clean sweep (ASan-O2 default-inlining failure above) but production (-O3) is clean and the failure is narrowly isolated to that inlining decision. **KEPT and promoted** — `g_av_fuse` defaults to 1. Both exact GQA-fused kernels (QK and AV) now complete. Next, if authorized: Phase 5 (softmax), only if material. Full writeup in `codex_recs_1.md` §22.31. |
| 2026-07-28 | Attention optimization Phase 5: exact softmax RVV vectorization — KEPT | 15e0df6 | Continuing autonomously per explicit authorization. Re-profile found softmax now the largest attention sub-bucket (43.5ms at ctx=256) now that QK/AV shrank the buckets around it. Added `rvv_max_f32`/`rvv_scale_f32` (RVV max-reduction and normalization only; expf stays scalar/exact, no approximate exponential); `QWEN_SOFTMAX_RVV=0` as the scalar revert. Validation: standalone probe bit-exact (2,155 cmp incl. n=1 and non-multiple-of-32 tails); production integration 32,740,245 cmp max_abs=0. Sanitizers found nothing new — same two already-characterized Phase 4.2 findings reappear unchanged (ASan-O2 inlining in av8_chunk, UBSan-O1 in pre-existing qh[] line), neither touching softmax's code; production (-O3) clean under both. | softmax / wall / tok/s vs Phase 4.2, short-context check | **Softmax summed-work: -8.5% (short), -13.9% (ctx=128), -15.0% (ctx=512), -15.6% (ctx=1024). Wall time: 0%, -0.5%, -1.2%, -2.2%.** Modest — smaller than QK/AV since softmax is what's left after two much bigger fusions — but reproducible, token-identical at every context, zero short-context regression, meeting the explicit Phase 5 gate ("reproducible end-to-end win with identical tokens and ≤2% short-context regression"). **KEPT and promoted** — `g_softmax_rvv` defaults to 1. All three attention sub-buckets (QK, AV, softmax) now optimized. Next, per authorization: Phase 6 (eight-core attention). Full writeup in `codex_recs_1.md` §22.32. |
| 2026-07-28 | Attention optimization Phase 6: eight-core attention — KEPT | 3ac5f30 | Continuing autonomously per explicit authorization. Linear/IME stays on 4 workers (harts 8/10/12/14); attention gained paired harts 9/11/13/15, splitting each KV head's 8 query heads across 2 sub-workers. Generalized pool sizing: `g_lin_nt=min(nt,4)` caps linear dispatch, `g_pool_nt=max(nt,g_attn_nt)` sizes the pool. Found and fixed a genuine bug (not a compiler quirk): ASan-O3 caught a 100%-reproducible stack-buffer-overflow in the AV output-pointer population loop, root-caused to per-iteration array re-declaration with a runtime-bounded loop; hoisting the arrays to function scope fixed it (0/15 post-fix vs 10/10 pre-fix). Post-fix sanitizers: ASan clean 9/9 across O1/O2/O3; UBSan clean 6/6 at O2/O3; UBSan-O1 shows this file's already-documented O1 GCC/RVV instability, not new. Production integration validation (QWEN_WORKERS_VALIDATE=1): bit-exact vs serial baseline at every worker count (55,050,240 cmp, 0 mismatches). Board temp 60-70°C throughout, no throttling. | attention / wall / tok/s vs Phase 5, dispatch overhead, temperature check | **Attention bucket: -36.2% (short), -46.0% (ctx=128), -44.6% (ctx=512), -39.3% (ctx=1024). Wall time: -0.4%, -4.5%, -10.8%, -14.9%. tok/s: +0.3%, +4.7%, +12.2%, +17.5%.** Improvement grows with context, dispatch overhead flat (0.08-0.14ms), tokens identical, no short-context regression -- eight-worker attention beats four workers reproducibly, meeting the explicit Phase 6 gate. **KEPT and promoted** — `g_attn_nt` defaults to 8; `QWEN_ATTN_NT=4`/`QWEN_ATTN_NT=1` are the four-worker/serial reverts. Canonical short HEADLINE moved again: ~12.3-12.35→12.37 tok/s. Phases 1-6 of the attention-optimization branch now complete. Full writeup in `codex_recs_1.md` §22.33. |
| 2026-07-28 | Exact-path closure: re-profile + one evidence-supported residual — CLOSED, no candidate cleared its gate | 5245bdf | Continuing autonomously. Re-profile at attn_nt=8 default: attention negligible at short context (<1.3% of wall), still subordinate to linear/MoE at ctx=1024 (34.5ms vs 61.9ms). Of 4 named residuals: KV cache already page-aligned via glibc mmap (no work needed); scratch allocation already lazy/reused; KV prefetch unsupported by evidence (head-major layout already sequential, dispatch overhead flat); next-layer prefetch has no safe window given MoE's data-dependent expert routing (same cross-layer dependency the directive warned against). Implemented and tested the one evidence-supported candidate: `g_scratch_align`/`QWEN_SCRATCH_ALIGN`, 64-byte posix_memalign vs plain malloc for attention scratch buffers. | wall/token short+ctx512+ctx1024, align=0 vs align=1 | **No reproducible win: -0.8% (short), -0.4% (ctx=512), +0.35% (ctx=1024, wrong direction).** Within trial-to-trial noise, does not clear any reasonable gate. **NOT KEPT** — `g_scratch_align` stays 0 by default, flag preserved as documented tested-and-rejected option. **Exact M=1 decode path declared closed** per the explicit "declare closed when no candidate clears its gate" instruction. Next, per broader authorization: M-batch/continuous batching. Full writeup in `codex_recs_1.md` §22.34. |
| 2026-07-28 | M-batch track milestone 1: vendor M4 kernel ported and validated | 782ad2a | Confirmed with user first, given the scale jump vs prior tuning-flag phases (a genuine new vendor-kernel port). Found `gemm_kernel_i8i4_hp_m4` already exists+used in production vendor llama.cpp -- a port, not novel design. `block_q4_0` confirmed zp-free (`block_type_has_zp==false`), so ported the no-zp branch. Ground truth A-record format from vendor's own packer `quantize_a_4row_i8_hp`: key finding is the per-subblock quantization scale is shared across all 4 batched rows (max abs jointly), not per-row -- M4 output is NOT expected bit-identical to 4x M1 calls, a principled quantization tradeoff not a bug. Caught+fixed a probe methodology bug: an exact-fp32-activation oracle produced misleading ~100%+ "error" for both M1 and M4 (conflated A-quantization noise with kernel correctness); switched to vendor_ime_a2_full.c's own proven "reconstruct from actual stored quantized bytes" method. | mean_abs_err vs own reconstructed reference, M1 vs M4, 200 trials/25600 comparisons | **M4 vs its own reference: 0.119% mean error, ratio 0.999x vs M1's own 0.119%** -- the port is numerically correct, introduces no extra error beyond ordinary quantization noise. M1-vs-M4 direct diff is real and nonzero (mean 0.041, max 0.24) as expected from the shared-scale design. Kernel-port milestone complete (`bench/vendor_ime_m4_probe.c`); remaining: production dispatch wiring, real multi-sequence batched-decode harness, per-sequence validation against separate M=1, aggregate/per-sequence tok/s + latency + memory + M=1-regression reporting. Full writeup in `codex_recs_1.md` §22.35. |
| 2026-07-28 | M-batch track milestone 2: real 4-sequence batched decode, dense layers only | ae8f8f5 | Scoped to dense layers (QKV/O/router/lm_head) after confirming with user -- MoE expert FFN needs a genuinely different per-expert gather/scatter mechanism (each sequence independently selects its own top-8/128 experts), separate undertaking. Built `forward4_dense_batch` (new `kind==3` pool dispatch reusing the M1 N-tile machinery) + a real test (`QWEN_MBATCH_TEST=1`, 4 distinct real prompts). Caught 2 real integration bugs before trusting results: att packed with wrong K (`d` instead of `ly->o.K`, qd-wide not d-wide), and a missing `pack_act_hp` call the MoE-FFN path needs. | token agreement / per-step divergence / decode-only tok/s, M=1 (4 separate) vs M=4 (1 batched), 2 trials + ASan/UBSan | **Token agreement over 16 gen steps: 70.31% -- root-caused via staged diagnostic (not hand-waved): ONE M4-batched 48-layer pass on identical input shows small divergence (max_abs=0.73/151936-wide vocab) with all 4 sequences' argmax choices matching exactly; full-generation gap is ordinary autoregressive compounding once a close argmax call flips in this hard-top-8-of-128 MoE model (2/4 sequences track M1 bit-for-bit all 16 steps, other 2 diverge from one specific step onward).** ASan/UBSan clean, correctness bit-identical across plain/ASan/UBSan builds. Existing single-sequence M=1 path unaffected (12.38-12.41 tok/s). **Decode-only speedup: 1.111x aggregate**, reproducible -- real but modest (M4's own arithmetic scales with M, amortizes weight-stream reads not raw compute; batched layers are only ~half the linear bucket, MoE-FFN untouched). ~222KB extra memory. **Status: validated, working, NOT wired into production default** -- no single-sequence call site benefits from batching. Remaining: MoE expert-FFN batching, real serving loop, batched prefill. Full writeup in `codex_recs_1.md` §22.36. |
| 2026-07-28 | M-batch track milestone 3: MoE expert-FFN batching -- implemented, measured NOT materially worthwhile at N=4 | cfac060 | Per explicit direction: group sequences per selected expert, batch via M4 when all 4 overlap exactly; partial 2-3-way overlap deliberately falls back to per-sequence M1, not padded (milestone 2's own finding that M4 arithmetic scales with M made padding a plausible net loss). Built esel[e][s]/ecount[e] grouping in forward4_dense_batch, every (sequence,slot) guaranteed handled exactly once (batched or fallback). ASan/UBSan clean, correctness bit-identical across builds, existing M=1 path unaffected. | selecting-sequence-count histogram + decode-only tok/s, M=1 vs M=4-with-expert-batching, ASan/UBSan | **Only 0.89% of expert-selection slots hit the batchable 4-way-overlap case (81% zero overlap, 15%/3% partial 2-/3-way overlap). Decode-only speedup: 1.103-1.105x -- statistically indistinguishable from milestone 2's dense-layer-only 1.111x.** **Verdict: expert-FFN batching adds no measurable benefit at N=4** -- overlap is fundamentally too rare at this batch size, and even full partial-overlap coverage wouldn't proportionally help given M4's per-row cost. Larger N would plausibly change this (birthday-paradox-style) but needs a real serving loop to validate, separate undertaking. **Closes the M-batch track's core empirical question**: dense-layer batching gives ~1.11x at N=4, expert-FFN batching adds nothing further. Full writeup in `codex_recs_1.md` §22.37. |
| 2026-07-28 | Batched prefill: chunked-M4 prefill within one sequence -- KEPT as validated opt-in, NOT promoted to production default | 3e6b98e | Explicit direction, prioritized over larger-N serving. Reframed M-batching across 4 consecutive positions of one sequence's own prompt (known upfront, no autoregressive dependency) rather than independent sequences -- dense layers batched via the validated M4 kernel, attention stays per-position/exact, MoE-FFN reuses milestone 3's same-expert grouping. First-run crash root-caused to a wrong invocation (cache path passed where the raw .gguf belongs), not a code bug -- confirmed via dmesg+addr2line pointing at gguf_open, not the new code. `prefill_chunk4`/`run_prefill_test` (`QWEN_PREFILL_TEST=1`) added; wired into `main()`'s real production prefill loop behind `QWEN_PREFILL_CHUNK` (default 0). | token agreement / final-logit diff / speedup at N=16/19/128/512/1024, sequential vs chunked-M4, ASan/UBSan, MoE-FFN ablation | **Token agreement 96-100% (vs milestone 2's decode-time 70.31%) -- prefill has no argmax-feedback compounding, divergence is purely M4's quantization noise. Speedup: consistent 1.10-1.14x across all 5 lengths, no length-dependent degradation.** N=19 (not a multiple of 4) validates the remainder-fallback path. ASan/UBSan clean (one methodology fix along the way: LeakSanitizer's raw `_exit()` was discarding unflushed stdout, fixed with explicit `fflush()` + `detect_leaks=0` for the pre-existing, always-present, by-design long-lived-buffer leaks). **Ablation (`QWEN_PREFILL_MOE4=0`): expert-FFN grouping contributes nothing measurable beyond dense-layer batching** (1.10-1.14x either way) despite a 6.8-9.4% within-prompt overlap rate -- 8-10x milestone 3's cross-sequence 0.89% -- a second, stronger confirmation of milestone 3's verdict. **KEPT as validated capability, smoke-tested end-to-end in production main(), but NOT promoted to default** -- unlike router/SwiGLU (which needed a full NLL/perplexity harness before promotion), this track's own validation standard hasn't included that harness, and since batched prefill (unlike decode M-batching) is a genuine single-user production lever, flipping the default is a real semantics-changing decision held for explicit confirmation. Full writeup in `codex_recs_1.md` §22.38. |
| 2026-07-28 | Batched prefill quality harness + clean production A/B -- PROMOTED to production default | c84409e |
| 2026-07-28 | Speculative/n-gram verification: instrumentation + acceptance-rate oracle -- general content likely a net loss, repetitive content a modest real win | 5c8e9d1 | Explicit direction: begin with instrumentation and an acceptance-rate oracle before integrating a draft mechanism. Built `ngram_propose` (pure host-side "prompt lookup" drafting, no draft model -- searches token history backward for the most recent prior occurrence of the last n tokens, proposes what followed it) + `run_speculative_oracle` (`QWEN_SPEC_ORACLE=1`): 5 real 384-token free-running greedy trajectories (4 general prompts spanning factual/reasoning/code/multilingual + one deliberately repetitive prompt, hp9 tiled 4x), swept n-gram length (2,3) x draft width (1,2,3,4,6,8), measured real acceptance against the trajectories' own actual content. ASan/UBSan clean, bit-identical across builds. | mean accepted tokens/round by content type and (n,K); back-of-envelope speedup projection using already-measured chunked-prefill cost data | **General/novel content tops out ~0.5-0.7 accepted/round at best parameters (code/fibonacci worst at 0.14-0.27); REPETITIVE (tiled hp9 x4) gets near-total acceptance: 3.00/3 at K=3,n=3, 7.93/8 at K=8.** A K=3+1-anchor verify round is structurally identical to prefill_chunk4's own mechanism (already measured: ~3.5-3.6x the cost of one ordinary decode step, not 4x-for-free per §22.36's "M4 scales with M" finding) -- breakeven needs >2.57/3 accepted just to match plain decoding. **Projected: general content ~2.3-2.9x SLOWER than plain decoding (clear net loss), repetitive content ~1.12x faster (real but modest, same M4-scaling ceiling as every other M-batch technique this session).** Directly answers, and FAILS for the general case, the ladder's own predeclared gate ("keep only if useful-token throughput improves outside copy-heavy prompts") -- the limitation is structural (n-gram lookup can't predict genuinely novel continuations), not a tunable parameter. Flagged for explicit user decision on how to proceed before any batched-verify mechanism is built. Full writeup in `codex_recs_1.md` §22.40. | Explicit direction: run the full multi-prompt NLL/perplexity harness with thresholds predeclared consistent with router/SwiGLU promotions; if it passes, run a clean production A/B at 128/512/1024 and promote with an explicit revert flag; do not begin speculative decoding or another track until closed. Built `run_prefill_quality_harness` (`QWEN_PREFILL_HARNESS=1`) reusing the router/SwiGLU harness scaffolding -- reference held at unchanged production config (int8 router + rational-Pade SwiGLU + sequential prefill), candidate differs only in HOW the prompt is ingested (chunked-M4 via the already-validated §22.38 mechanism), teacher-forced divergence/NLL plus real-text perplexity over the same 9 corpora used for SwiGLU's own real-text gate. | teacher-forced perplexity multiplier / divergence / real-text aggregate+per-corpus multiplier / production A/B at 128,512,1024 | **All 5 predeclared gates PASS: teacher-forced perplexity x1.0156 (1.6%, <1.05), divergence 3.3% (25/760, <15%), real-text aggregate x0.9968 (essentially neutral, <1.05), worst corpus x1.0130 (<1.10), speed 1.10-1.14x (already measured, >=10%).** Divergence sits below both router's (2.1%) and SwiGLU's (3.2%) own promotion-time numbers despite touching every layer of the whole prompt. Clean production A/B (real binary, QWEN_CTXLEN=128/512/1024, 2 paired interleaved trials/length): ~10-12% real prefill speedup at every length, decode bucket statistically flat, first generated token bit-identical between configs at every length/trial. **PROMOTED**: `g_prefill_chunk` default 0->1, `QWEN_PREFILL_CHUNK=0` is the explicit sequential revert. Verified with zero env overrides: `' Tokyo'` PASS, 12.43 tok/s, matches HEADLINE within noise. Closes the decision gate the user held the ladder on -- speculative/n-gram verification may now begin. Full writeup in `codex_recs_1.md` §22.39. |
| | R3 | | model=… | tok/s | spacemit llama vs pure-C |
| | | | | | |

---

## 13. Bottom line (updated 2026-07-26 — Path A is done, not "next" anymore)

**Superseded by events — kept for history, see the note at the bottom of this section.** Path A
(§4, the vendor-shaped N32 native-int4 kernel) was pursued through A1-A5, ported, verified, and
fully integrated as `qwen_moe_hp.c`: **1.49 → 8.84-9.25 tok/s (~6.1x)**, closing in on the real
vendor binary's 11.71-12.89. Current status and open branches: `codex_recs_1.md` §22,
`PROGRESS.md` HEADLINE.

1. **Decode remains weight-bandwidth-bound**, and the *original* 1.36-1.49 tok/s engine (our own
   q4-in-q8 interleave, `qwen_moe.c`) had substantial non-stream overhead on top of that — now
   measured, not guessed (§21-22). Most of that overhead is closed in `qwen_moe_hp.c`; attention
   and activation-packing (both former top-3 buckets) are now vectorized and closed out (§22.9,
   §22.10); `linear(kernel)` (58.5ms, ~52% of wall) is the sole remaining large bucket, extensively
   investigated (§22.11-§22.13) — see item 2 below.
2. Path A is done (not "next"). The real router-as-HP-Lin test (through `lin_mm_hp`, pooled and
   multi-threaded — not the earlier single-threaded fp16 experiment) is **also done**, and so is
   the vendor-style int8-M1 router that follows it: real but modest speedups (int4: router
   18.7→12.5ms, +3-4% tok/s; int8: router →16.0ms, +2-4% tok/s) traded against routing quality that
   doesn't cleanly hold for int4 (60.3% of decisions get >=1 expert swapped vs fp32) but holds much
   better for int8 (7.8%, ~8x fewer perturbed decisions). **Decision recorded *at the time this
   paragraph was written***: fp32 routing was the default in `qwen_moe_hp.c` (`g_router_mode=0`);
   int4-HP (`g_router_mode=1`) and int8-M1 (`g_router_mode=2`) were both kept behind the same
   experimental flag, neither promoted. **This is now stale — superseded twice over**: a
   multi-prompt quality harness later promoted int8-M1 to the production default
   (`g_router_mode` 0→2, §22.15), and a rational-Padé SwiGLU approximation was subsequently
   promoted too (`g_swiglu_fast` 0→2, §22.20-§22.21) — see the §12 log and `PROGRESS.md`'s
   HEADLINE for the actual current defaults. See `codex_recs_1.md` §22.7-§22.8 for the original
   router writeup, including two real bugs found along the way (unsafe argmax sentinel for raw
   logits; a timing-boundary mismatch of the same class as §21's). Remaining open items, ranked:
   `linear(kernel)` itself — its expert-FFN subclass is 61% of the bucket (§22.10 follow-up), and
   the bandwidth investigation (§22.11-§22.13) closed out with one actionable-but-unapplied
   candidate (blocked panel scheduling for `lm_head`, tested in production and **reverted** after
   regressing — see `PROGRESS.md`'s cold-access section and `codex_recs_1.md` §22.14). SwiGLU,
   listed here as deferred when this paragraph was first written, is no longer open — see above.
3. Our **q4-in-q8 interleave remains a valid, working baseline** (`qwen_moe.c`, untouched,
   1.49 tok/s) — kept for comparison, not deleted.
4. Record probe numbers here; promote winners into `codex_recs_1.md` as ordered findings only
   after they falsify or confirm a stated hypothesis with real measurements — "loop-carried
   latency" in the router row above is a live example of a hypothesis that hasn't been isolated
   yet and shouldn't be treated as confirmed.

---

*Living document. Update §12 and the “Result (fill in)” cells; do not rewrite history of failed probes — keep them so we do not re-chase dead paths.*
